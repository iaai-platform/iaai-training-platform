<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>On-Demand Courses - International Aesthetic Academic Institution (IAAI)</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f4f4f4;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Header Placeholder */
        .header-placeholder {
            height: 80px;
            background: #002147;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ffcc00;
            font-size: 18px;
            font-weight: 600;
        }

        /* Breadcrumb */
        .breadcrumb {
            background: linear-gradient(135deg, #f8f9fa, #ffffff);
            padding: 20px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .breadcrumb-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .breadcrumb a {
            color: #002147;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .breadcrumb a:hover {
            color: #ffcc00;
        }

        .breadcrumb span {
            color: #666;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, #002147 0%, #003366 100%);
            padding: 100px 0;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            opacity: 0.3;
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            text-align: center;
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-size: 48px;
            font-weight: 800;
            color: #ffffff;
            margin-bottom: 20px;
            line-height: 1.2;
        }

        .hero p {
            font-size: 20px;
            color: #e0e0e0;
            font-weight: 400;
            max-width: 800px;
            margin: 0 auto 40px;
        }

        .hero-features {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 40px;
            flex-wrap: wrap;
        }

        .hero-feature {
            display: flex;
            align-items: center;
            gap: 10px;
            color: #e0e0e0;
            font-size: 16px;
            font-weight: 500;
        }

        .hero-feature i {
            color: #ffcc00;
            font-size: 20px;
        }

        /* Main Content */
        .main-content {
            max-width: 1200px;
            margin: 80px auto;
            padding: 0 20px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: start;
        }

        /* Course Description Section */
        .course-description {
            background: #ffffff;
            border-radius: 25px;
            box-shadow: 0 25px 80px rgba(0, 0, 0, 0.1);
            padding: 60px;
            position: relative;
            overflow: hidden;
        }

        .course-description::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, #ffcc00, #b8860b, #002147);
        }

        .course-description h2 {
            font-size: 36px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 25px;
            position: relative;
        }

        .course-description h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 60px;
            height: 4px;
            background: linear-gradient(90deg, #ffcc00, #b8860b);
        }

        .course-description p {
            font-size: 18px;
            color: #555;
            line-height: 1.8;
            margin-bottom: 30px;
        }

        .course-description h3 {
            font-size: 24px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 20px;
            margin-top: 40px;
        }

        .topics-list {
            list-style: none;
            padding: 0;
        }

        .topics-list li {
            background: #f8f9fa;
            margin-bottom: 15px;
            padding: 20px;
            border-radius: 15px;
            border-left: 5px solid #ffcc00;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .topics-list li:hover {
            background: #e3f2fd;
            transform: translateX(5px);
        }

        .topics-list li i {
            color: #ffcc00;
            font-size: 20px;
            width: 25px;
            text-align: center;
        }

        .topics-list li span {
            font-weight: 600;
            color: #002147;
        }

        /* Benefits Section */
        .benefits-section {
            margin-top: 40px;
        }

        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .benefit-card {
            background: linear-gradient(135deg, #f8f9fa, #ffffff);
            padding: 25px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease;
        }

        .benefit-card:hover {
            transform: translateY(-5px);
        }

        .benefit-card i {
            font-size: 32px;
            color: #ffcc00;
            margin-bottom: 15px;
        }

        .benefit-card h4 {
            font-size: 16px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 10px;
        }

        .benefit-card p {
            font-size: 14px;
            color: #666;
            margin: 0;
        }

        /* Application Form Section */
        .application-form {
            background: #ffffff;
            border-radius: 25px;
            box-shadow: 0 25px 80px rgba(0, 0, 0, 0.1);
            padding: 60px;
            position: relative;
            overflow: hidden;
        }

        .application-form::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, #002147, #ffcc00, #b8860b);
        }

        .application-form h2 {
            font-size: 36px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 25px;
            position: relative;
        }

        .application-form h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 60px;
            height: 4px;
            background: linear-gradient(90deg, #ffcc00, #b8860b);
        }

        .form-intro {
            font-size: 16px;
            color: #666;
            margin-bottom: 40px;
            line-height: 1.6;
        }

        /* Form Styling */
        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            font-size: 16px;
            font-weight: 600;
            color: #002147;
            margin-bottom: 8px;
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 16px;
            font-family: 'Montserrat', sans-serif;
            transition: all 0.3s ease;
            background: #f8f9fa;
        }

        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: #ffcc00;
            background: #ffffff;
            box-shadow: 0 0 20px rgba(255, 204, 0, 0.2);
        }

        .submit-btn {
            background: linear-gradient(135deg, #ffcc00, #b8860b);
            color: #002147;
            padding: 18px 40px;
            border: none;
            border-radius: 50px;
            font-size: 18px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(255, 204, 0, 0.3);
            letter-spacing: 1px;
            text-transform: uppercase;
            width: 100%;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(255, 204, 0, 0.5);
            background: linear-gradient(135deg, #b8860b, #ffcc00);
        }

        .submit-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        /* Success/Error Messages */
        .message {
            padding: 15px 20px;
            border-radius: 10px;
            margin-bottom: 25px;
            font-weight: 500;
            display: none;
        }

        .message.success {
            background: #d4edda;
            color: #155724;
            border: 2px solid #c3e6cb;
        }

        .message.error {
            background: #f8d7da;
            color: #721c24;
            border: 2px solid #f5c6cb;
        }

        /* Course Preview Section */
        .course-preview {
            background: linear-gradient(135deg, #002147, #003366);
            padding: 80px 0;
            margin: 80px 0;
            color: #ffffff;
        }

        .preview-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .section-title {
            text-align: center;
            margin-bottom: 50px;
        }

        .section-title h2 {
            font-size: 42px;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 20px;
        }

        .section-title p {
            font-size: 18px;
            color: #e0e0e0;
            max-width: 600px;
            margin: 0 auto;
        }

        .courses-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .course-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 40px 30px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: transform 0.3s ease;
            text-align: center;
        }

        .course-card:hover {
            transform: translateY(-10px);
        }

        .course-card .course-icon {
            font-size: 48px;
            color: #ffcc00;
            margin-bottom: 20px;
        }

        .course-card h4 {
            font-size: 20px;
            font-weight: 700;
            margin-bottom: 15px;
            color: #ffffff;
        }

        .course-card p {
            font-size: 14px;
            color: #e0e0e0;
            margin-bottom: 20px;
            line-height: 1.6;
        }

        .course-price {
            font-size: 18px;
            font-weight: 700;
            color: #ffcc00;
            margin-bottom: 20px;
        }

        .preview-btn {
            background: transparent;
            border: 2px solid #ffcc00;
            color: #ffcc00;
            padding: 10px 25px;
            border-radius: 25px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .preview-btn:hover {
            background: #ffcc00;
            color: #002147;
        }

        /* Responsive Design */
        @media (max-width: 968px) {
            .main-content {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .hero h1 {
                font-size: 36px;
            }

            .hero-features {
                flex-direction: column;
                gap: 20px;
            }

            .course-description,
            .application-form {
                padding: 40px 30px;
            }

            .course-description h2,
            .application-form h2 {
                font-size: 28px;
            }
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 28px;
            }

            .hero p {
                font-size: 16px;
            }

            .course-description,
            .application-form {
                padding: 30px 20px;
            }

            .benefits-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Animation */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in {
            animation: fadeInUp 0.6s ease-out;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <!-- Breadcrumb -->
    <nav class="breadcrumb">
        <div class="breadcrumb-container">
            <a href="/">Home</a> > <a href="/training-programs">Training Programs</a> > <span>On-Demand Courses</span>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>On-Demand Aesthetic Training</h1>
            <p>Learn at your own pace with our comprehensive library of expert-led aesthetic medicine courses. Access world-class training anytime, anywhere, with lifetime access and certification upon completion.</p>
            <div class="hero-features">
                <div class="hero-feature">
                    <i class="fas fa-clock"></i>
                    <span>Learn at Your Own Pace</span>
                </div>
                <div class="hero-feature">
                    <i class="fas fa-certificate"></i>
                    <span>Internationally Certified</span>
                </div>
                <div class="hero-feature">
                    <i class="fas fa-mobile-alt"></i>
                    <span>Mobile & Desktop Access</span>
                </div>
                <div class="hero-feature">
                    <i class="fas fa-infinity"></i>
                    <span>Lifetime Access</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Course Description Section -->
        <div class="course-description fade-in">
            <h2>Why Choose On-Demand Training?</h2>
            <p>Our on-demand courses provide the flexibility to learn aesthetic medicine techniques at your own pace, with access to the same high-quality content delivered by our internationally recognized experts.</p>
            
            <h3>Popular Course Topics:</h3>
            <ul class="topics-list">
                <li>
                    <i class="fas fa-syringe"></i>
                    <span>Botulinum Toxin & Dermal Fillers</span>
                </li>
                <li>
                    <i class="fas fa-face-smile"></i>
                    <span>Advanced Facial Contouring</span>
                </li>
                <li>
                    <i class="fas fa-head-side-mask"></i>
                    <span>Non-Surgical Rhinoplasty</span>
                </li>
                <li>
                    <i class="fas fa-spa"></i>
                    <span>PRP Therapy & Skin Rejuvenation</span>
                </li>
                <li>
                    <i class="fas fa-eye"></i>
                    <span>Advanced Eye Area Treatments</span>
                </li>
                <li>
                    <i class="fas fa-lips"></i>
                    <span>Lip Enhancement Techniques</span>
                </li>
            </ul>

            <div class="benefits-section">
                <h3>Course Benefits:</h3>
                <div class="benefits-grid">
                    <div class="benefit-card">
                        <i class="fas fa-play-circle"></i>
                        <h4>HD Video Content</h4>
                        <p>Crystal clear demonstrations</p>
                    </div>
                    <div class="benefit-card">
                        <i class="fas fa-download"></i>
                        <h4>Downloadable Resources</h4>
                        <p>PDF guides and materials</p>
                    </div>
                    <div class="benefit-card">
                        <i class="fas fa-question-circle"></i>
                        <h4>Expert Q&A</h4>
                        <p>Direct access to instructors</p>
                    </div>
                    <div class="benefit-card">
                        <i class="fas fa-chart-line"></i>
                        <h4>Progress Tracking</h4>
                        <p>Monitor your learning journey</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Application Form Section -->
        <div class="application-form fade-in">
            <h2>Enroll in On-Demand Courses</h2>
            <p class="form-intro">Complete the form below to get started with our on-demand training programs. Our team will contact you within 24 hours with course access details and payment information.</p>
            
            <div id="messageContainer" class="message"></div>
            
            <form id="onDemandForm">
                <div class="form-group">
                    <label for="fullName">Full Name *</label>
                    <input type="text" id="fullName" name="fullName" placeholder="Enter your full name" required>
                </div>

                <div class="form-group">
                    <label for="email">Email Address *</label>
                    <input type="email" id="email" name="email" placeholder="Enter your email address" required>
                </div>

                <div class="form-group">
                    <label for="phoneNumber">Phone Number *</label>
                    <input type="tel" id="phoneNumber" name="phoneNumber" placeholder="Enter your phone number" required>
                </div>

                <div class="form-group">
                    <label for="profession">Medical Profession</label>
                    <select id="profession" name="profession">
                        <option value="">Select your profession</option>
                        <option value="doctor">Medical Doctor</option>
                        <option value="nurse">Nurse Practitioner</option>
                        <option value="dentist">Dentist</option>
                        <option value="dermatologist">Dermatologist</option>
                        <option value="plastic-surgeon">Plastic Surgeon</option>
                        <option value="student">Medical Student</option>
                        <option value="other">Other Healthcare Professional</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="experience">Years of Experience</label>
                    <select id="experience" name="experience">
                        <option value="">Select experience level</option>
                        <option value="0-1">0-1 years</option>
                        <option value="2-5">2-5 years</option>
                        <option value="6-10">6-10 years</option>
                        <option value="10+">10+ years</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="courseInterest">Primary Course of Interest *</label>
                    <select id="courseInterest" name="courseInterest" required>
                        <option value="">Select a course</option>
                        <option value="botox">Botox & Dermal Fillers</option>
                        <option value="facial-contouring">Advanced Facial Contouring</option>
                        <option value="rhinoplasty">Non-Surgical Rhinoplasty</option>
                        <option value="skin-rejuvenation">Skin Rejuvenation</option>
                        <option value="prp">PRP Therapy</option>
                        <option value="eye-treatments">Eye Area Treatments</option>
                        <option value="lip-enhancement">Lip Enhancement</option>
                        <option value="complete-package">Complete Course Package</option>
                    </select>
                </div>

                <button type="submit" class="submit-btn" id="submitBtn">
                    Submit Application
                </button>
            </form>
        </div>
    </main>

    <!-- Course Preview Section -->
    <section class="course-preview">
        <div class="preview-container">
            <div class="section-title">
                <h2>Featured On-Demand Courses</h2>
                <p>Explore our most popular self-paced training programs designed for aesthetic medicine professionals</p>
            </div>
            <div class="courses-grid">
                <div class="course-card fade-in">
                    <div class="course-icon"><i class="fas fa-syringe"></i></div>
                    <h4>Foundation Fillers Mastery</h4>
                    <p>Complete course covering injection techniques, facial anatomy, and safety protocols for dermal fillers.</p>
                    <div class="course-price">From $399</div>
                    <a href="#" class="preview-btn">Preview Course</a>
                </div>
                <div class="course-card fade-in">
                    <div class="course-icon"><i class="fas fa-face-smile"></i></div>
                    <h4>Advanced Botox Techniques</h4>
                    <p>Master upper and lower face Botox applications with advanced injection patterns and patient assessment.</p>
                    <div class="course-price">From $499</div>
                    <a href="#" class="preview-btn">Preview Course</a>
                </div>
                <div class="course-card fade-in">
                    <div class="course-icon"><i class="fas fa-spa"></i></div>
                    <h4>Skin Rejuvenation Complete</h4>
                    <p>Comprehensive training in PRP, mesotherapy, chemical peels, and advanced skin treatment protocols.</p>
                    <div class="course-price">From $599</div>
                    <a href="#" class="preview-btn">Preview Course</a>
                </div>
                <div class="course-card fade-in">
                    <div class="course-icon"><i class="fas fa-head-side-mask"></i></div>
                    <h4>Non-Surgical Rhinoplasty</h4>
                    <p>Advanced techniques for nose reshaping using dermal fillers, including safety considerations and complications.</p>
                    <div class="course-price">From $799</div>
                    <a href="#" class="preview-btn">Preview Course</a>
                </div>
            </div>
        </div>
    </section>

    <%- include('partials/footer') %>

    <script>
        // Enhanced form functionality
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('onDemandForm');
            const submitBtn = document.getElementById('submitBtn');
            const messageContainer = document.getElementById('messageContainer');

            // Show message function
            function showMessage(message, type) {
                messageContainer.textContent = message;
                messageContainer.className = `message ${type}`;
                messageContainer.style.display = 'block';
                
                // Auto-hide success messages after 5 seconds
                if (type === 'success') {
                    setTimeout(() => {
                        messageContainer.style.display = 'none';
                    }, 5000);
                }
            }

            // Form submission handler
            form.addEventListener('submit', async function(event) {
                event.preventDefault();
                
                // Disable submit button and show loading state
                submitBtn.disabled = true;
                submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Submitting...';
                
                // Hide previous messages
                messageContainer.style.display = 'none';
                
                const formData = {
                    fullName: document.getElementById('fullName').value,
                    email: document.getElementById('email').value,
                    phoneNumber: document.getElementById('phoneNumber').value,
                    profession: document.getElementById('profession').value,
                    experience: document.getElementById('experience').value,
                    courseInterest: document.getElementById('courseInterest').value,
                };

                try {
                    const response = await fetch('/apply-on-demand', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(formData),
                    });

                    const result = await response.json();
                    
                    if (result.success) {
                        showMessage('✅ Application submitted successfully! Our team will contact you within 24 hours with course access details.', 'success');
                        form.reset();
                    } else {
                        showMessage('❌ ' + (result.message || 'Application submission failed. Please try again.'), 'error');
                    }
                } catch (error) {
                    console.error('Error submitting application:', error);
                    showMessage('❌ Network error. Please check your connection and try again.', 'error');
                }
                
                // Re-enable submit button
                submitBtn.disabled = false;
                submitBtn.innerHTML = 'Submit Application';
            });

            // Form validation enhancements
            const requiredFields = ['fullName', 'email', 'phoneNumber', 'courseInterest'];
            
            requiredFields.forEach(fieldId => {
                const field = document.getElementById(fieldId);
                field.addEventListener('blur', function() {
                    if (this.value.trim() === '') {
                        this.style.borderColor = '#dc3545';
                    } else {
                        this.style.borderColor = '#28a745';
                    }
                });
            });

            // Email validation
            document.getElementById('email').addEventListener('blur', function() {
                const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                if (!emailRegex.test(this.value) && this.value !== '') {
                    this.style.borderColor = '#dc3545';
                    showMessage('Please enter a valid email address.', 'error');
                } else if (this.value !== '') {
                    this.style.borderColor = '#28a745';
                }
            });

            // Phone number formatting
            document.getElementById('phoneNumber').addEventListener('input', function(e) {
                let value = e.target.value.replace(/\D/g, '');
                if (value.length > 0) {
                    if (value.startsWith('971') || value.startsWith('90')) {
                        e.target.value = '+' + value;
                    } else {
                        e.target.value = value;
                    }
                }
            });

            // Intersection Observer for animations
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('fade-in');
                    }
                });
            }, observerOptions);

            // Observe all fade-in elements
            document.querySelectorAll('.fade-in').forEach(el => {
                observer.observe(el);
            });

            // Preview button functionality
            document.querySelectorAll('.preview-btn').forEach(btn => {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    const courseName = this.closest('.course-card').querySelector('h4').textContent;
                    showMessage(`Course preview for "${courseName}" will be available soon. Please submit the application form to get notified when previews are ready.`, 'success');
                });
            });
        });
    </script>
</body>
</html>