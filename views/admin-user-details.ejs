<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details - IAAI Admin</title>
    <link rel="stylesheet" href="/css/admin.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        /* User Details Page Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: #f8f9fa;
            color: #333;
            line-height: 1.6;
        }

        /* Admin Header */
        .admin-header {
            background: linear-gradient(135deg, #dc3545, #c82333);
            color: white;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 5px 15px rgba(220,53,69,0.2);
        }

        .admin-header-content {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .admin-header h1 {
            font-size: 2rem;
            font-weight: 700;
            margin: 0;
        }

        .breadcrumb {
            background: rgba(255,255,255,0.1);
            padding: 10px 15px;
            border-radius: 25px;
            font-size: 0.9rem;
        }

        .breadcrumb a {
            color: white;
            text-decoration: none;
            opacity: 0.8;
        }

        .breadcrumb a:hover {
            opacity: 1;
            text-decoration: underline;
        }

        /* Container */
        .admin-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* User Profile Card */
        .user-profile-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            padding: 30px;
            margin-bottom: 30px;
        }

        .profile-header {
            display: flex;
            align-items: center;
            gap: 30px;
            padding-bottom: 25px;
            border-bottom: 2px solid #f0f2f5;
            margin-bottom: 25px;
        }

        .user-avatar-large {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            font-weight: 700;
            box-shadow: 0 5px 15px rgba(0,123,255,0.3);
            overflow: hidden;
        }

        .user-avatar-large img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .user-info-main h2 {
            margin: 0 0 10px 0;
            font-size: 1.8rem;
            color: #333;
        }

        .user-email {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 10px;
        }

        .user-badges {
            display: flex;
            gap: 10px;
            margin-top: 10px;
            flex-wrap: wrap;
        }

        .badge {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .badge-role {
            background: #17a2b8;
            color: white;
        }

        .badge-role.admin {
            background: #dc3545;
        }

        .badge-role.instructor {
            background: #6f42c1;
        }

        .badge-status {
            background: #28a745;
            color: white;
        }

        .badge-status.pending {
            background: #ffc107;
            color: #333;
        }

        .badge-success {
            background: #28a745;
            color: white;
        }

        .badge-info {
            background: #17a2b8;
            color: white;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 0.75rem;
            display: inline-block;
            margin: 2px;
        }

        /* Info Grid */
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .info-item {
            padding: 15px;
            background: #f8f9fa;
            border-radius: 10px;
            border-left: 4px solid #007bff;
        }

        .info-label {
            font-size: 0.85rem;
            color: #666;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 5px;
        }

        .info-value {
            font-size: 1.1rem;
            font-weight: 500;
            color: #333;
        }

        /* Professional Profile Section */
        .professional-section {
            background: #e8f4f8;
            padding: 20px;
            border-radius: 10px;
            margin-top: 20px;
            border-left: 4px solid #17a2b8;
        }

        .professional-section h4 {
            color: #17a2b8;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .professional-section h5 {
            font-size: 1.1rem;
            margin: 20px 0 15px 0;
            color: #17a2b8;
            border-bottom: 1px solid #e9ecef;
            padding-bottom: 8px;
        }

        /* Section Cards */
        .section-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            padding: 25px;
            margin-bottom: 25px;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid #f0f2f5;
        }

        .section-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: #333;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .section-title i {
            color: #007bff;
        }

        /* Course List */
        .course-list {
            display: grid;
            gap: 15px;
        }

        .course-item {
            padding: 20px;
            background: #f8f9fa;
            border-radius: 10px;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            transition: all 0.3s ease;
        }

        .course-item:hover {
            background: #e9ecef;
            transform: translateX(5px);
        }

        .course-details {
            flex: 1;
        }

        .course-details h4 {
            margin: 0 0 8px 0;
            color: #333;
            font-size: 1.1rem;
        }

        .course-meta {
            display: flex;
            gap: 15px;
            font-size: 0.9rem;
            color: #666;
            flex-wrap: wrap;
            margin-bottom: 10px;
        }

        .course-meta span {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .course-status-info {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            gap: 8px;
            flex-shrink: 0;
        }

        .course-type-badge {
            padding: 4px 10px;
            border-radius: 15px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            white-space: nowrap;
        }

        .type-in-person {
            background: #e3f2fd;
            color: #1976d2;
        }

        .type-online-live {
            background: #f3e5f5;
            color: #7b1fa2;
        }

        .type-self-paced {
            background: #e8f5e9;
            color: #388e3c;
        }

        .status-pill {
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 600;
            white-space: nowrap;
        }

        .status-paid {
            background: #d4edda;
            color: #155724;
        }

        .status-cart {
            background: #fff3cd;
            color: #856404;
        }

        .status-verified {
            background: #d4edda;
            color: #155724;
        }

        .status-pending {
            background: #fff3cd;
            color: #856404;
        }

        .status-rejected {
            background: #f8d7da;
            color: #721c24;
        }

        .status-not-submitted {
            background: #e2e3e5;
            color: #383d41;
        }

        .status-not-started {
            background: #e9ecef;
            color: #495057;
        }

        .status-in-progress {
            background: #cce7ff;
            color: #004085;
        }

        .status-completed {
            background: #d4edda;
            color: #155724;
        }

        /* Progress Section */
        .progress-section {
            margin-top: 15px;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 8px;
            border-left: 4px solid #007bff;
        }

        .progress-section small {
            display: block;
            margin-bottom: 5px;
            color: #666;
        }

        .progress-bar-container {
            background: #e0e0e0;
            height: 10px;
            border-radius: 5px;
            overflow: hidden;
            margin: 10px 0;
        }

        .progress-bar-fill {
            height: 100%;
            background: #4caf50;
            transition: width 0.3s ease;
        }

        .progress-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 10px;
            margin: 10px 0;
        }

        /* Action Buttons */
        .action-bar {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            padding: 20px;
            margin-bottom: 30px;
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }

        .btn {
            padding: 12px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: #007bff;
            color: white;
        }

        .btn-primary:hover {
            background: #0056b3;
        }

        .btn-success {
            background: #28a745;
            color: white;
        }

        .btn-success:hover {
            background: #218838;
        }

        .btn-warning {
            background: #ffc107;
            color: #333;
        }

        .btn-warning:hover {
            background: #e0a800;
        }

        .btn-danger {
            background: #dc3545;
            color: white;
        }

        .btn-danger:hover {
            background: #c82333;
        }

        .btn-secondary {
            background: #6c757d;
            color: white;
        }

        .btn-secondary:hover {
            background: #545b62;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 40px;
            color: #6c757d;
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 15px;
            opacity: 0.5;
        }

        /* Timeline */
        .activity-timeline {
            position: relative;
            padding-left: 30px;
        }

        .activity-timeline::before {
            content: '';
            position: absolute;
            left: 10px;
            top: 0;
            bottom: 0;
            width: 2px;
            background: #e9ecef;
        }

        .timeline-item {
            position: relative;
            padding-bottom: 20px;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: -24px;
            top: 5px;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #007bff;
            border: 2px solid white;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .timeline-content {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
        }

        .timeline-date {
            font-size: 0.85rem;
            color: #666;
            margin-bottom: 5px;
        }

        /* Settings Grid */
        .settings-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .setting-card {
            padding: 20px;
            background: #f8f9fa;
            border-radius: 10px;
            border: 1px solid #e9ecef;
        }

        .setting-card h5 {
            margin: 0 0 15px 0;
            color: #333;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .setting-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
        }

        .setting-item:last-child {
            border-bottom: none;
        }

        .document-preview {
            width: 80px;
            height: 80px;
            border-radius: 8px;
            object-fit: cover;
            border: 2px solid #e9ecef;
            margin-top: 10px;
        }

        .certificate-highlight {
            background: linear-gradient(135deg, #ffd700, #ffed4e);
            padding: 10px;
            border-radius: 8px;
            margin-top: 10px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .profile-header {
                flex-direction: column;
                text-align: center;
            }

            .info-grid {
                grid-template-columns: 1fr;
            }

            .course-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 15px;
            }

            .course-status-info {
                align-items: flex-start;
                width: 100%;
                flex-direction: row;
                gap: 10px;
            }

            .action-bar {
                flex-direction: column;
            }

            .btn {
                width: 100%;
                justify-content: center;
            }

            .settings-grid {
                grid-template-columns: 1fr;
            }

            .progress-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<%- include('partials/header') %>

<body>
    <!-- Admin Header -->
    <div class="admin-header">
        <div class="admin-header-content">
            <div>
                <h1><i class="fas fa-user"></i> User Details</h1>
                <p>View and manage comprehensive user information</p>
            </div>
            <div class="breadcrumb">
                <a href="/">Home</a> > 
                <a href="/dashboard">Dashboard</a> > 
                <a href="/admin-users">Users</a> > 
                <span>User Details</span>
            </div>
        </div>
    </div>

    <div class="admin-container">
        <!-- Action Bar -->
        <div class="action-bar">
            <a href="/admin-users" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to Users
            </a>
            <% if (!userDetails.isConfirmed) { %>
                <button class="btn btn-success" onclick="approveUser('<%= userDetails._id %>')">
                    <i class="fas fa-check"></i> Approve User
                </button>
            <% } %>
            <button class="btn btn-warning" onclick="resetPassword('<%= userDetails._id %>')">
                <i class="fas fa-key"></i> Reset Password
            </button>
            <button class="btn btn-primary" onclick="changeRole('<%= userDetails._id %>', '<%= userDetails.role %>')">
                <i class="fas fa-user-tag"></i> Change Role
            </button>
            <button class="btn btn-danger" onclick="deleteUser('<%= userDetails._id %>', '<%= userDetails.firstName %> <%= userDetails.lastName %>')">
                <i class="fas fa-trash"></i> Delete User
            </button>
        </div>

        <!-- User Profile Card -->
        <div class="user-profile-card">
            <div class="profile-header">
                <div class="user-avatar-large">
                    <% if (userDetails.profileInfo && userDetails.profileInfo.profilePicture && userDetails.profileInfo.profilePicture.url) { %>
                        <img src="<%= userDetails.profileInfo.profilePicture.url %>" alt="Profile Picture">
                    <% } else { %>
                        <%= userDetails.firstName.charAt(0) %><%= userDetails.lastName.charAt(0) %>
                    <% } %>
                </div>
                <div class="user-info-main">
                    <h2>
                        <% if (userDetails.professionalInfo && userDetails.professionalInfo.basic && userDetails.professionalInfo.basic.title && userDetails.professionalInfo.basic.title !== 'Not specified') { %>
                            <%= userDetails.professionalInfo.basic.title %> 
                        <% } %>
                        <%= userDetails.firstName %> <%= userDetails.lastName %>
                    </h2>
                    <p class="user-email"><%= userDetails.email %></p>
                    <div class="user-badges">
                        <span class="badge badge-role <%= userDetails.role %>"><%= userDetails.role %></span>
                        <span class="badge badge-status <%= userDetails.isConfirmed ? '' : 'pending' %>">
                            <%= userDetails.isConfirmed ? 'Confirmed' : 'Pending Confirmation' %>
                        </span>
                        <% if (userDetails.accountInfo && userDetails.accountInfo.twoFactorAuth && userDetails.accountInfo.twoFactorAuth.enabled) { %>
                            <span class="badge badge-success">2FA Enabled</span>
                        <% } %>
                        <% if (userDetails.profileInfo && userDetails.profileInfo.identificationDocument && userDetails.profileInfo.identificationDocument.verificationStatus === 'verified') { %>
                            <span class="badge badge-success">ID Verified</span>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- Basic Information -->
            <div class="info-grid">
                <div class="info-item">
                    <div class="info-label">Phone Number</div>
                    <div class="info-value"><%= userDetails.phoneNumber || 'Not provided' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Country</div>
                    <div class="info-value"><%= userDetails.country || 'Not specified' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Profession</div>
                    <div class="info-value"><%= userDetails.profession || 'Not specified' %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Member Since</div>
                    <div class="info-value"><%= new Date(userDetails.createdAt).toLocaleDateString() %></div>
                </div>
            </div>

            <!-- User Statistics -->
            <% if (userDetails.userStats) { %>
                <div class="professional-section">
                    <h4><i class="fas fa-chart-line"></i> User Statistics</h4>
                    <div class="info-grid">
                        <div class="info-item">
                            <div class="info-label">Total Spent</div>
                            <div class="info-value">$<%= userDetails.userStats.totalSpent.toFixed(2) %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Total Transactions</div>
                            <div class="info-value"><%= userDetails.userStats.totalTransactions %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Courses Enrolled</div>
                            <div class="info-value"><%= userDetails.userStats.totalCourses %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Courses Completed</div>
                            <div class="info-value"><%= userDetails.userStats.completedCourses %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Courses In Progress</div>
                            <div class="info-value"><%= userDetails.userStats.inProgressCourses %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Certificates Earned</div>
                            <div class="info-value"><%= userDetails.userStats.certificatesEarned %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Average Progress</div>
                            <div class="info-value"><%= userDetails.userStats.averageProgress %>%</div>
                        </div>
                    </div>
                </div>
            <% } %>

            <!-- Enhanced Professional Information -->
            <% if (userDetails.professionalInfo) { %>
                <div class="professional-section">
                    <h4><i class="fas fa-briefcase-medical"></i> Professional Information</h4>
                    <div class="info-grid">
                        <div class="info-item">
                            <div class="info-label">Title</div>
                            <div class="info-value"><%= userDetails.professionalInfo.basic.title %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Field of Study</div>
                            <div class="info-value"><%= userDetails.professionalInfo.basic.fieldOfStudy %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Specialty</div>
                            <div class="info-value"><%= userDetails.professionalInfo.basic.specialty %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Aesthetics Experience</div>
                            <div class="info-value"><%= userDetails.professionalInfo.basic.aestheticsExperience %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Years of Experience</div>
                            <div class="info-value"><%= userDetails.professionalInfo.basic.yearsOfExperience %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Current Workplace</div>
                            <div class="info-value"><%= userDetails.professionalInfo.basic.currentWorkplace %></div>
                        </div>
                    </div>
                    
                    <!-- License Information -->
                    <% if (userDetails.professionalInfo.license.hasLicense) { %>
                        <h5><i class="fas fa-certificate"></i> License Information</h5>
                        <div class="info-grid">
                            <div class="info-item">
                                <div class="info-label">License Number</div>
                                <div class="info-value"><%= userDetails.professionalInfo.license.licenseNumber %></div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">License State</div>
                                <div class="info-value"><%= userDetails.professionalInfo.license.licenseState %></div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">License Country</div>
                                <div class="info-value"><%= userDetails.professionalInfo.license.licenseCountry %></div>
                            </div>
                        </div>
                    <% } %>

                    <!-- Areas of Interest -->
                    <% if (userDetails.professionalInfo.interests && userDetails.professionalInfo.interests.length > 0) { %>
                        <h5><i class="fas fa-star"></i> Areas of Interest</h5>
                        <div style="margin-top: 10px;">
                            <% userDetails.professionalInfo.interests.forEach(interest => { %>
                                <span class="badge badge-info"><%= interest %></span>
                            <% }) %>
                        </div>
                    <% } %>

                    <!-- Training Goals -->
                    <% if (userDetails.professionalInfo.basic.trainingGoals && userDetails.professionalInfo.basic.trainingGoals !== 'Not specified') { %>
                        <h5><i class="fas fa-target"></i> Training Goals</h5>
                        <div style="margin-top: 10px;">
                            <div class="info-value"><%= userDetails.professionalInfo.basic.trainingGoals %></div>
                        </div>
                    <% } %>
                </div>
            <% } %>

            <!-- Legacy Professional Profile (fallback) -->
            <% if (userDetails.professionalProfile && (userDetails.professionalProfile.clinicName || userDetails.professionalProfile.bio)) { %>
                <div class="professional-section">
                    <h4><i class="fas fa-briefcase-medical"></i> Legacy Professional Profile</h4>
                    <div class="info-grid">
                        <% if (userDetails.professionalProfile.clinicName) { %>
                            <div class="info-item">
                                <div class="info-label">Clinic Name</div>
                                <div class="info-value"><%= userDetails.professionalProfile.clinicName %></div>
                            </div>
                        <% } %>
                        <% if (userDetails.professionalProfile.licenseNumber) { %>
                            <div class="info-item">
                                <div class="info-label">License Number</div>
                                <div class="info-value"><%= userDetails.professionalProfile.licenseNumber %></div>
                            </div>
                        <% } %>
                        <% if (userDetails.professionalProfile.yearsExperience) { %>
                            <div class="info-item">
                                <div class="info-label">Years of Experience</div>
                                <div class="info-value"><%= userDetails.professionalProfile.yearsExperience %></div>
                            </div>
                        <% } %>
                    </div>
                    <% if (userDetails.professionalProfile.bio) { %>
                        <div style="margin-top: 15px;">
                            <div class="info-label">Bio</div>
                            <div class="info-value"><%= userDetails.professionalProfile.bio %></div>
                        </div>
                    <% } %>
                </div>
            <% } %>
        </div>

        <!-- Profile & Documentation -->
        <div class="section-card">
            <div class="section-header">
                <h3 class="section-title">
                    <i class="fas fa-id-card"></i> Profile & Documentation
                </h3>
                <span class="badge badge-primary">
                    <%= userDetails.profileInfo && userDetails.profileInfo.completionStatus ? userDetails.profileInfo.completionStatus.overallPercentage : 0 %>% Complete
                </span>
            </div>

            <div class="settings-grid">
                <!-- Profile Picture -->
                <div class="setting-card">
                    <h5><i class="fas fa-camera"></i> Profile Picture</h5>
                    <% if (userDetails.profileInfo && userDetails.profileInfo.profilePicture && userDetails.profileInfo.profilePicture.url) { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span style="color: #28a745;">✓ Uploaded</span>
                        </div>
                        <div class="setting-item">
                            <span>Upload Date</span>
                            <span><%= new Date(userDetails.profileInfo.profilePicture.uploadDate).toLocaleDateString() %></span>
                        </div>
                        <div class="setting-item">
                            <span>File Size</span>
                            <span><%= (userDetails.profileInfo.profilePicture.fileSize / 1024).toFixed(2) %> KB</span>
                        </div>
                        <div style="margin-top: 10px;">
                            <img src="<%= userDetails.profileInfo.profilePicture.url %>" alt="Profile" class="document-preview">
                        </div>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span style="color: #dc3545;">✗ Not uploaded</span>
                        </div>
                    <% } %>
                </div>

                <!-- Identification Document -->
                <div class="setting-card">
                    <h5><i class="fas fa-passport"></i> Identification Document</h5>
                    <% if (userDetails.profileInfo && userDetails.profileInfo.identificationDocument && userDetails.profileInfo.identificationDocument.url) { %>
                        <div class="setting-item">
                            <span>Document Type</span>
                            <span><%= userDetails.profileInfo.identificationDocument.documentType || 'Not specified' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Upload Date</span>
                            <span><%= new Date(userDetails.profileInfo.identificationDocument.uploadDate).toLocaleDateString() %></span>
                        </div>
                        <div class="setting-item">
                            <span>Verification Status</span>
                            <span class="status-pill status-<%= userDetails.profileInfo.identificationDocument.verificationStatus %>">
                                <%= userDetails.profileInfo.identificationDocument.verificationStatus %>
                            </span>
                        </div>
                        <% if (userDetails.profileInfo.identificationDocument.verificationNotes) { %>
                            <div style="margin-top: 10px;">
                                <div class="info-label">Verification Notes</div>
                                <div class="info-value"><%= userDetails.profileInfo.identificationDocument.verificationNotes %></div>
                            </div>
                        <% } %>
                        <% if (userDetails.profileInfo.identificationDocument.verifiedBy) { %>
                            <div class="setting-item">
                                <span>Verified By</span>
                                <span><%= userDetails.profileInfo.identificationDocument.verifiedBy %></span>
                            </div>
                        <% } %>
                        <% if (userDetails.profileInfo.identificationDocument.verifiedAt) { %>
                            <div class="setting-item">
                                <span>Verified At</span>
                                <span><%= new Date(userDetails.profileInfo.identificationDocument.verifiedAt).toLocaleDateString() %></span>
                            </div>
                        <% } %>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span style="color: #dc3545;">✗ Not uploaded</span>
                        </div>
                    <% } %>
                </div>

                <!-- Professional Documents -->
                <div class="setting-card">
                    <h5><i class="fas fa-file-alt"></i> Professional Documents</h5>
                    <% if (userDetails.profileInfo && userDetails.profileInfo.professionalDocuments && userDetails.profileInfo.professionalDocuments.length > 0) { %>
                        <% userDetails.profileInfo.professionalDocuments.forEach(doc => { %>
                            <div class="setting-item">
                                <span><%= doc.type.toUpperCase() %></span>
                                <span><%= new Date(doc.uploadDate).toLocaleDateString() %></span>
                            </div>
                            <% if (doc.description) { %>
                                <div style="margin-top: 5px; font-size: 0.85rem; color: #666;">
                                    <%= doc.description %>
                                </div>
                            <% } %>
                        <% }) %>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span style="color: #6c757d;">No documents uploaded</span>
                        </div>
                    <% } %>
                </div>

                <!-- Profile Completion -->
                <div class="setting-card">
                    <h5><i class="fas fa-tasks"></i> Profile Completion</h5>
                    <% if (userDetails.profileInfo && userDetails.profileInfo.completionStatus) { %>
                        <div class="setting-item">
                            <span>Basic Info</span>
                            <span><%= userDetails.profileInfo.completionStatus.basicInfo ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Professional Info</span>
                            <span><%= userDetails.profileInfo.completionStatus.professionalInfo ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Profile Picture</span>
                            <span><%= userDetails.profileInfo.completionStatus.profilePicture ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>ID Document</span>
                            <span><%= userDetails.profileInfo.completionStatus.identificationDocument ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Last Updated</span>
                            <span><%= new Date(userDetails.profileInfo.completionStatus.lastUpdated).toLocaleDateString() %></span>
                        </div>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span>Not tracked</span>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Enhanced Course Progress Details -->
        <div class="section-card">
            <div class="section-header">
                <h3 class="section-title">
                    <i class="fas fa-graduation-cap"></i> Enrolled Courses
                </h3>
                <span class="badge badge-primary"><%= userDetails.myCourses ? userDetails.myCourses.length : 0 %> Courses</span>
            </div>

            <% if (userDetails.myCourses && userDetails.myCourses.length > 0) { %>
                <div class="course-list">
                    <% userDetails.myCourses.forEach(course => { %>
                        <div class="course-item">
                            <div class="course-details">
                                <h4><%= course.courseTitle || course.title %></h4>
                                <div class="course-meta">
                                    <span><i class="fas fa-code"></i> <%= course.courseCode %></span>
                                    <span><i class="fas fa-dollar-sign"></i> $<%= course.price || course.paidAmount || 0 %></span>
                                    <span><i class="fas fa-calendar"></i> <%= new Date(course.registrationDate || course.dateOfRegistration).toLocaleDateString() %></span>
                                    <% if (course.promoCodeUsed) { %>
                                        <span><i class="fas fa-tag"></i> <%= course.promoCodeUsed %></span>
                                    <% } %>
                                    <% if (course.expiryDate) { %>
                                        <span><i class="fas fa-clock"></i> Expires: <%= new Date(course.expiryDate).toLocaleDateString() %></span>
                                    <% } %>
                                </div>

                                <!-- Enhanced Progress Details -->
                                <% if (course.courseType === 'Self-Paced') { %>
                                    <div class="progress-section">
                                        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px;">
                                            <span><strong>Course Progress</strong></span>
                                            <span><%= course.overallPercentage || 0 %>%</span>
                                        </div>
                                        <div class="progress-bar-container">
                                            <div class="progress-bar-fill" style="width: <%= course.overallPercentage || 0 %>%"></div>
                                        </div>
                                        <div class="progress-grid">
                                            <div><small><strong>Videos:</strong> <%= course.completedVideos || 0 %>/<%= course.totalVideos || 0 %></small></div>
                                            <div><small><strong>Exams:</strong> <%= course.completedExams || 0 %> completed</small></div>
                                            <div><small><strong>Watch Time:</strong> <%= course.totalWatchTime || 0 %> min</small></div>
                                            <div><small><strong>Avg Score:</strong> <%= course.averageExamScore || 0 %>%</small></div>
                                            <% if (course.lastAccessedAt) { %>
                                                <div><small><strong>Last Access:</strong> <%= new Date(course.lastAccessedAt).toLocaleDateString() %></small></div>
                                            <% } %>
                                            <div><small><strong>Notes:</strong> <%= course.videoNotes || 0 %></small></div>
                                            <div><small><strong>Bookmarks:</strong> <%= course.bookmarks || 0 %></small></div>
                                        </div>
                                    </div>
                                <% } else if (course.courseType === 'Online Live') { %>
                                    <div class="progress-section">
                                        <div class="progress-grid">
                                            <div><small><strong>Attendance:</strong> <%= course.attendancePercentage || 0 %>%</small></div>
                                            <div><small><strong>Sessions:</strong> <%= course.sessionsAttended || 0 %> attended</small></div>
                                            <div><small><strong>Recordings:</strong> <%= course.recordingsWatched || 0 %> watched</small></div>
                                            <div><small><strong>Attempts:</strong> <%= course.assessmentAttempts || 0 %> assessments</small></div>
                                            <% if (course.assessmentScore) { %>
                                                <div><small><strong>Best Score:</strong> <%= course.assessmentScore %>%</small></div>
                                            <% } %>
                                        </div>
                                    </div>
                                <% } else if (course.courseType === 'In-Person') { %>
                                    <div class="progress-section">
                                        <div class="progress-grid">
                                            <div><small><strong>Attendance:</strong> <%= course.attendancePercentage || 0 %>%</small></div>
                                            <% if (course.assessmentScore) { %>
                                                <div><small><strong>Assessment:</strong> <%= course.assessmentScore %>%</small></div>
                                            <% } %>
                                            <% if (course.completionDate) { %>
                                                <div><small><strong>Completed:</strong> <%= new Date(course.completionDate).toLocaleDateString() %></small></div>
                                            <% } %>
                                        </div>
                                    </div>
                                <% } %>

                                <!-- Certificate Info -->
                                <% if (course.certificateId) { %>
                                    <div class="certificate-highlight">
                                        <small style="color: #856404;"><i class="fas fa-certificate"></i> <strong>Certificate ID:</strong> <%= course.certificateId %></small>
                                    </div>
                                <% } %>
                            </div>
                            <div class="course-status-info">
                                <span class="course-type-badge type-<%= course.courseType.toLowerCase().replace(' ', '-') %>">
                                    <%= course.courseType %>
                                </span>
                                <span class="status-pill <%= course.enrollmentStatus === 'paid' || course.status === 'Paid and Registered' ? 'status-paid' : 'status-cart' %>">
                                    <%= course.enrollmentStatus || course.status %>
                                </span>
                                <% if (course.courseStatus) { %>
                                    <span class="status-pill status-<%= course.courseStatus %>">
                                        <%= course.courseStatus %>
                                    </span>
                                <% } %>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <i class="fas fa-book"></i>
                    <p>No courses enrolled yet</p>
                </div>
            <% } %>
        </div>

        <!-- Wishlist -->
        <div class="section-card">
            <div class="section-header">
                <h3 class="section-title">
                    <i class="fas fa-heart"></i> Wishlist
                </h3>
                <span class="badge badge-primary"><%= userDetails.myWishlist ? userDetails.myWishlist.length : 0 %> Items</span>
            </div>

            <% if (userDetails.myWishlist && userDetails.myWishlist.length > 0) { %>
                <div class="course-list">
                    <% userDetails.myWishlist.forEach(course => { %>
                        <div class="course-item">
                            <div class="course-details">
                                <h4><%= course.courseTitle || course.title %></h4>
                                <div class="course-meta">
                                    <span><i class="fas fa-code"></i> <%= course.courseCode %></span>
                                    <span><i class="fas fa-dollar-sign"></i> $<%= course.price %></span>
                                </div>
                            </div>
                            <span class="course-type-badge type-<%= course.courseType.toLowerCase().replace(' ', '-') %>">
                                <%= course.courseType %>
                            </span>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <i class="fas fa-heart"></i>
                    <p>No items in wishlist</p>
                </div>
            <% } %>
        </div>

        <!-- Certificates -->
        <% if (userDetails.myCertificates && userDetails.myCertificates.length > 0) { %>
            <div class="section-card">
                <div class="section-header">
                    <h3 class="section-title">
                        <i class="fas fa-certificate"></i> Certificates
                    </h3>
                    <span class="badge badge-success"><%= userDetails.myCertificates.length %> Certificates</span>
                </div>

                <div class="course-list">
                    <% userDetails.myCertificates.forEach(cert => { %>
                        <div class="course-item">
                            <div class="course-details">
                                <h4><%= cert.certificateData.courseTitle %></h4>
                                <div class="course-meta">
                                    <span><i class="fas fa-code"></i> <%= cert.certificateData.courseCode %></span>
                                    <span><i class="fas fa-id-badge"></i> <%= cert.certificateId %></span>
                                    <span><i class="fas fa-calendar-check"></i> Completed: <%= new Date(cert.certificateData.completionDate).toLocaleDateString() %></span>
                                    <span><i class="fas fa-calendar-plus"></i> Issued: <%= new Date(cert.certificateData.issueDate).toLocaleDateString() %></span>
                                    <% if (cert.certificateData.expiryDate) { %>
                                        <span><i class="fas fa-calendar-times"></i> Expires: <%= new Date(cert.certificateData.expiryDate).toLocaleDateString() %></span>
                                    <% } %>
                                </div>

                                <!-- Achievement Details -->
                                <% if (cert.certificateData.achievement) { %>
                                    <div class="progress-section">
                                        <div class="progress-grid">
                                            <% if (cert.certificateData.achievement.attendancePercentage) { %>
                                                <div><small><strong>Attendance:</strong> <%= cert.certificateData.achievement.attendancePercentage %>%</small></div>
                                            <% } %>
                                            <% if (cert.certificateData.achievement.examScore) { %>
                                                <div><small><strong>Exam Score:</strong> <%= cert.certificateData.achievement.examScore %>%</small></div>
                                            <% } %>
                                            <% if (cert.certificateData.achievement.totalHours) { %>
                                                <div><small><strong>Total Hours:</strong> <%= cert.certificateData.achievement.totalHours %></small></div>
                                            <% } %>
                                            <% if (cert.certificateData.achievement.grade) { %>
                                                <div><small><strong>Grade:</strong> <%= cert.certificateData.achievement.grade %></small></div>
                                            <% } %>
                                        </div>
                                    </div>
                                <% } %>

                                <!-- Download & Verification Info -->
                                <div style="margin-top: 10px; padding: 10px; background: #f0f8ff; border-radius: 5px;">
                                    <div class="progress-grid">
                                        <div><small><strong>Downloads:</strong> <%= cert.downloadCount || 0 %></small></div>
                                        <% if (cert.lastDownloaded) { %>
                                            <div><small><strong>Last Downloaded:</strong> <%= new Date(cert.lastDownloaded).toLocaleDateString() %></small></div>
                                        <% } %>
                                        <div><small><strong>Public:</strong> <%= cert.isPublic ? 'Yes' : 'No' %></small></div>
                                        <% if (cert.certificateData.verificationCode) { %>
                                            <div><small><strong>Verification:</strong> <%= cert.certificateData.verificationCode %></small></div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                            <div class="course-status-info">
                                <span class="course-type-badge type-<%= cert.courseType.toLowerCase().replace('training', '').replace('online', 'online-') %>">
                                    <%= cert.courseType.replace('Training', '') %>
                                </span>
                                <span class="status-pill status-paid">Valid</span>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        <% } %>

        <!-- User Preferences & Learning Settings -->
        <div class="section-card">
            <div class="section-header">
                <h3 class="section-title">
                    <i class="fas fa-cog"></i> User Preferences & Learning Settings
                </h3>
            </div>

            <div class="settings-grid">
                <!-- Library Preferences -->
                <div class="setting-card">
                    <h5><i class="fas fa-book-open"></i> Library Preferences</h5>
                    <% if (userDetails.userPreferences && userDetails.userPreferences.library) { %>
                        <div class="setting-item">
                            <span>Default View</span>
                            <span><%= userDetails.userPreferences.library.defaultView || 'grid' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Sort By</span>
                            <span><%= userDetails.userPreferences.library.sortBy || 'recent' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Show Completed</span>
                            <span><%= userDetails.userPreferences.library.showCompleted !== false ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Favorites</span>
                            <span><%= userDetails.userPreferences.library.favorites ? userDetails.userPreferences.library.favorites.length : 0 %></span>
                        </div>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span>Default settings</span>
                        </div>
                    <% } %>
                </div>

                <!-- Learning Preferences -->
                <div class="setting-card">
                    <h5><i class="fas fa-play"></i> Learning Preferences</h5>
                    <% if (userDetails.userPreferences && userDetails.userPreferences.learning) { %>
                        <div class="setting-item">
                            <span>Video Quality</span>
                            <span><%= userDetails.userPreferences.learning.videoQuality || 'auto' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Playback Speed</span>
                            <span><%= userDetails.userPreferences.learning.playbackSpeed || 1 %>x</span>
                        </div>
                        <div class="setting-item">
                            <span>Autoplay</span>
                            <span><%= userDetails.userPreferences.learning.autoplay !== false ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Subtitles</span>
                            <span><%= userDetails.userPreferences.learning.subtitles ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Language</span>
                            <span><%= userDetails.userPreferences.learning.language || 'en' %></span>
                        </div>
                    <% } else if (userDetails.learningPreferences) { %>
                        <!-- Legacy support -->
                        <div class="setting-item">
                            <span>Pace</span>
                            <span><%= userDetails.learningPreferences.pace || 'normal' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Video Quality</span>
                            <span><%= userDetails.learningPreferences.videoQuality || 'auto' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Subtitles</span>
                            <span><%= userDetails.learningPreferences.subtitles ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Language</span>
                            <span><%= userDetails.learningPreferences.language || 'en' %></span>
                        </div>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span>Default settings</span>
                        </div>
                    <% } %>
                </div>

                <!-- Notification Settings -->
                <div class="setting-card">
                    <h5><i class="fas fa-bell"></i> Notifications</h5>
                    <% if (userDetails.userPreferences && userDetails.userPreferences.notifications) { %>
                        <div class="setting-item">
                            <span>Email Notifications</span>
                            <span><%= userDetails.userPreferences.notifications.email !== false ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Course Updates</span>
                            <span><%= userDetails.userPreferences.notifications.courseUpdates !== false ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Promotions</span>
                            <span><%= userDetails.userPreferences.notifications.promotions ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Reminders</span>
                            <span><%= userDetails.userPreferences.notifications.reminders !== false ? '✓' : '✗' %></span>
                        </div>
                    <% } else if (userDetails.notificationSettings) { %>
                        <!-- Legacy support -->
                        <div class="setting-item">
                            <span>Email Notifications</span>
                            <span><%= userDetails.notificationSettings.email ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Course Updates</span>
                            <span><%= userDetails.notificationSettings.courseUpdates ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Promotions</span>
                            <span><%= userDetails.notificationSettings.promotions ? '✓' : '✗' %></span>
                        </div>
                        <div class="setting-item">
                            <span>Digest Frequency</span>
                            <span><%= userDetails.notificationSettings.digest || 'weekly' %></span>
                        </div>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span>Default settings</span>
                        </div>
                    <% } %>
                </div>

                <!-- Account Security -->
                <div class="setting-card">
                    <h5><i class="fas fa-shield-alt"></i> Account Security</h5>
                    <% if (userDetails.accountInfo && userDetails.accountInfo.status) { %>
                        <div class="setting-item">
                            <span>Account Status</span>
                            <span style="color: <%= userDetails.accountInfo.status.isActive !== false ? '#28a745' : '#dc3545' %>;">
                                <%= userDetails.accountInfo.status.isActive !== false ? 'Active' : 'Inactive' %>
                            </span>
                        </div>
                        <div class="setting-item">
                            <span>Account Locked</span>
                            <span style="color: <%= userDetails.accountInfo.status.isLocked ? '#dc3545' : '#28a745' %>;">
                                <%= userDetails.accountInfo.status.isLocked ? 'Yes' : 'No' %>
                            </span>
                        </div>
                        <div class="setting-item">
                            <span>Login Attempts</span>
                            <span><%= userDetails.accountInfo.status.loginAttempts || 0 %></span>
                        </div>
                        <% if (userDetails.accountInfo.status.lastLoginAt) { %>
                            <div class="setting-item">
                                <span>Last Login</span>
                                <span><%= new Date(userDetails.accountInfo.status.lastLoginAt).toLocaleString() %></span>
                            </div>
                        <% } %>
                    <% } else if (userDetails.accountStatus) { %>
                        <!-- Legacy support -->
                        <div class="setting-item">
                            <span>Account Status</span>
                            <span style="color: <%= userDetails.accountStatus.isActive !== false ? '#28a745' : '#dc3545' %>;">
                                <%= userDetails.accountStatus.isActive !== false ? 'Active' : 'Inactive' %>
                            </span>
                        </div>
                        <div class="setting-item">
                            <span>Account Locked</span>
                            <span style="color: <%= userDetails.accountStatus.isLocked ? '#dc3545' : '#28a745' %>;">
                                <%= userDetails.accountStatus.isLocked ? 'Yes' : 'No' %>
                            </span>
                        </div>
                        <% if (userDetails.accountStatus.lastLoginAt) { %>
                            <div class="setting-item">
                                <span>Last Login</span>
                                <span><%= new Date(userDetails.accountStatus.lastLoginAt).toLocaleString() %></span>
                            </div>
                        <% } %>
                    <% } else { %>
                        <div class="setting-item">
                            <span>Status</span>
                            <span>Standard account</span>
                        </div>
                    <% } %>
                    
                    <!-- Two-Factor Authentication -->
                    <% if (userDetails.accountInfo && userDetails.accountInfo.twoFactorAuth) { %>
                        <div class="setting-item">
                            <span>Two-Factor Auth</span>
                            <span style="color: <%= userDetails.accountInfo.twoFactorAuth.enabled ? '#28a745' : '#6c757d' %>;">
                                <%= userDetails.accountInfo.twoFactorAuth.enabled ? 'Enabled' : 'Disabled' %>
                            </span>
                        </div>
                    <% } else if (userDetails.twoFactorAuth) { %>
                        <!-- Legacy support -->
                        <div class="setting-item">
                            <span>Two-Factor Auth</span>
                            <span style="color: <%= userDetails.twoFactorAuth.enabled ? '#28a745' : '#6c757d' %>;">
                                <%= userDetails.twoFactorAuth.enabled ? 'Enabled' : 'Disabled' %>
                            </span>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Account Activity -->
        <div class="section-card">
            <div class="section-header">
                <h3 class="section-title">
                    <i class="fas fa-history"></i> Recent Activity
                </h3>
            </div>

            <div class="activity-timeline">
                <% if (userDetails.accountInfo && userDetails.accountInfo.status && userDetails.accountInfo.status.lastLoginAt) { %>
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <div class="timeline-date">
                                <%= new Date(userDetails.accountInfo.status.lastLoginAt).toLocaleString() %>
                            </div>
                            <div>Last login</div>
                        </div>
                    </div>
                <% } else if (userDetails.accountStatus && userDetails.accountStatus.lastLoginAt) { %>
                    <div class="timeline-item">
                        <div class="timeline-content">
                            <div class="timeline-date">
                                <%= new Date(userDetails.accountStatus.lastLoginAt).toLocaleString() %>
                            </div>
                            <div>Last login</div>
                        </div>
                    </div>
                <% } %>
                
                <% if (userDetails.accountInfo && userDetails.accountInfo.loginActivity && userDetails.accountInfo.loginActivity.length > 0) { %>
                    <% userDetails.accountInfo.loginActivity.slice(0, 5).forEach(activity => { %>
                        <div class="timeline-item">
                            <div class="timeline-content">
                                <div class="timeline-date">
                                    <%= new Date(activity.loginTime).toLocaleString() %>
                                </div>
                                <div>
                                    Login from <%= activity.location || 'Unknown location' %>
                                    <% if (activity.ip) { %>
                                        <small class="text-muted">(IP: <%= activity.ip %>)</small>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else if (userDetails.loginActivity && userDetails.loginActivity.length > 0) { %>
                    <% userDetails.loginActivity.slice(0, 5).forEach(activity => { %>
                        <div class="timeline-item">
                            <div class="timeline-content">
                                <div class="timeline-date">
                                    <%= new Date(activity.loginTime).toLocaleString() %>
                                </div>
                                <div>
                                    Login from <%= activity.location || 'Unknown location' %>
                                    <% if (activity.ip) { %>
                                        <small class="text-muted">(IP: <%= activity.ip %>)</small>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } %>
                
                <div class="timeline-item">
                    <div class="timeline-content">
                        <div class="timeline-date">
                            <%= new Date(userDetails.createdAt).toLocaleString() %>
                        </div>
                        <div>Account created</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Payment Transactions -->
        <% if (userDetails.paymentTransactions && userDetails.paymentTransactions.length > 0) { %>
            <div class="section-card">
                <div class="section-header">
                    <h3 class="section-title">
                        <i class="fas fa-credit-card"></i> Payment Transactions
                    </h3>
                    <span class="badge badge-primary"><%= userDetails.paymentTransactions.length %> Transactions</span>
                </div>

                <div class="course-list">
                    <% userDetails.paymentTransactions.slice(0, 10).forEach(transaction => { %>
                        <div class="course-item">
                            <div class="course-details">
                                <h4>Receipt #<%= transaction.receiptNumber %></h4>
                                <div class="course-meta">
                                    <span><i class="fas fa-calendar"></i> <%= new Date(transaction.transactionDate).toLocaleDateString() %></span>
                                    <span><i class="fas fa-credit-card"></i> <%= transaction.paymentMethod || 'Card' %></span>
                                    <span><i class="fas fa-dollar-sign"></i> Subtotal: $<%= transaction.subtotal.toFixed(2) %></span>
                                    <% if (transaction.discountAmount > 0) { %>
                                        <span><i class="fas fa-tag"></i> Discount: $<%= transaction.discountAmount.toFixed(2) %></span>
                                    <% } %>
                                    <span><i class="fas fa-money-bill"></i> Total: $<%= transaction.finalAmount.toFixed(2) %></span>
                                    <% if (transaction.promoCode) { %>
                                        <span><i class="fas fa-percent"></i> Promo: <%= transaction.promoCode %></span>
                                    <% } %>
                                </div>
                                
                                <!-- Transaction Items -->
                                <% if (transaction.items && transaction.items.length > 0) { %>
                                    <div style="margin-top: 10px; padding: 10px; background: #f0f8ff; border-radius: 5px;">
                                        <small><strong>Items purchased:</strong></small>
                                        <% transaction.items.forEach(item => { %>
                                            <div style="margin-top: 5px;">
                                                <small>• <%= item.courseType %> - $<%= item.paidPrice.toFixed(2) %></small>
                                            </div>
                                        <% }) %>
                                    </div>
                                <% } %>
                            </div>
                            <div class="course-status-info">
                                <span class="status-pill status-<%= transaction.paymentStatus === 'completed' ? 'paid' : transaction.paymentStatus %>">
                                    <%= transaction.paymentStatus %>
                                </span>
                                <span class="badge badge-info">
                                    <%= transaction.currency || 'USD' %>
                                </span>
                            </div>
                        </div>
                    <% }) %>
                    
                    <% if (userDetails.paymentTransactions.length > 10) { %>
                        <div style="text-align: center; padding: 20px; color: #666;">
                            <small>Showing 10 of <%= userDetails.paymentTransactions.length %> transactions</small>
                        </div>
                    <% } %>
                </div>
            </div>
        <% } %>
    </div>

    <script>
        // Approve user
        async function approveUser(userId) {
            const result = await Swal.fire({
                title: 'Approve User',
                text: 'Are you sure you want to approve this user?',
                icon: 'question',
                showCancelButton: true,
                confirmButtonColor: '#28a745',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'Yes, approve!'
            });

            if (result.isConfirmed) {
                try {
                    const response = await fetch('/admin/approve-user', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ 
                            userId: userId,
                            sendEmail: true  // Always send email
                        })
                    });

                    const data = await response.json();

                    if (data.success) {
                        await Swal.fire({
                            icon: 'success',
                            title: 'User Approved!',
                            text: data.message,
                            timer: 2000
                        });
                        location.reload();
                    } else {
                        Swal.fire('Error', data.message, 'error');
                    }
                } catch (error) {
                    Swal.fire('Error', 'Failed to approve user', 'error');
                }
            }
        }

        // Change user role
        async function changeRole(userId, currentRole) {
            const { value: newRole } = await Swal.fire({
                title: 'Change User Role',
                input: 'select',
                inputOptions: {
                    'user': 'User',
                    'instructor': 'Instructor',
                    'admin': 'Admin'
                },
                inputPlaceholder: 'Select a role',
                inputValue: currentRole,
                showCancelButton: true,
                confirmButtonColor: '#007bff',
                inputValidator: (value) => {
                    if (!value) {
                        return 'You need to select a role!'
                    }
                }
            });

            if (newRole && newRole !== currentRole) {
                try {
                    const response = await fetch(`/admin-users/${userId}/update-role`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ role: newRole })
                    });

                    const data = await response.json();

                    if (data.success) {
                        await Swal.fire({
                            icon: 'success',
                            title: 'Role Updated!',
                            text: `User role changed to ${newRole}`,
                            timer: 2000
                        });
                        location.reload();
                    }
                } catch (error) {
                    Swal.fire('Error', 'Failed to update role', 'error');
                }
            }
        }

        // Reset password
        async function resetPassword(userId) {
            const result = await Swal.fire({
                title: 'Reset Password',
                text: 'This will send a password reset email to the user. Continue?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#ffc107',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'Yes, reset!'
            });

            if (result.isConfirmed) {
                try {
                    const response = await fetch(`/admin-users/${userId}/reset-password`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    });

                    const data = await response.json();

                    if (data.success) {
                        Swal.fire({
                            icon: 'success',
                            title: 'Password Reset!',
                            text: data.message,
                            timer: 3000
                        });
                    }
                } catch (error) {
                    Swal.fire('Error', 'Failed to reset password', 'error');
                }
            }
        }

        // Delete user (Move to Recycle Bin)
        async function deleteUser(userId, userName) {
            const { value: formValues } = await Swal.fire({
                title: 'Delete User',
                html: `
                    <p>Are you sure you want to delete <strong>${userName}</strong>?</p>
                    <p class="text-muted">The user will be moved to recycle bin and can be recovered within 30 days.</p>
                    <div style="margin-top: 20px;">
                        <label for="deletion-reason" style="display: block; text-align: left; margin-bottom: 5px;">Reason for deletion (optional):</label>
                        <textarea id="deletion-reason" class="swal2-input" style="width: 100%; height: 80px;" placeholder="Enter reason..."></textarea>
                    </div>
                `,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#dc3545',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'Yes, delete!',
                cancelButtonText: 'Cancel',
                reverseButtons: true,
                preConfirm: () => {
                    return {
                        reason: document.getElementById('deletion-reason').value
                    }
                }
            });
            
            if (formValues) {
                try {
                    const response = await fetch('/admin/reject-user', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ 
                            userId,
                            reason: formValues.reason || 'Deleted by admin'
                        })
                    });
                    
                    const data = await response.json();
                    
                    if (data.success) {
                        await Swal.fire({
                            icon: 'success',
                            title: 'User Moved to Recycle Bin!',
                            text: data.message,
                            timer: 2000
                        });
                        window.location.href = '/admin-users';
                    } else {
                        Swal.fire('Error', data.message, 'error');
                    }
                } catch (error) {
                    Swal.fire('Error', 'Failed to delete user', 'error');
                }
            }
        }
    </script>

    <%- include('partials/footer') %>
</body>
</html>