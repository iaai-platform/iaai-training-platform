<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Why Choose IAAI - International Aesthetic Academic Institution</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f4f4f4;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Breadcrumb */
        .breadcrumb {
            background: linear-gradient(135deg, #f8f9fa, #ffffff);
            padding: 20px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .breadcrumb-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .breadcrumb a {
            color: #002147;
            text-decoration: none;
            font-weight: 500;
        }

        .breadcrumb a:hover {
            color: #ffcc00;
        }

        .breadcrumb span {
            color: #666;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, #002147 0%, #003366 100%);
            padding: 120px 0;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            opacity: 0.3;
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            text-align: center;
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-size: 48px;
            font-weight: 800;
            color: #ffffff;
            margin-bottom: 20px;
            line-height: 1.2;
        }

        .hero p {
            font-size: 20px;
            color: #e0e0e0;
            font-weight: 400;
            max-width: 800px;
            margin: 0 auto;
        }

        /* Statistics Section */
        .stats-section {
            background: #ffffff;
            padding: 80px 0;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        }

        .stats-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
        }

        .stat-item {
            text-align: center;
            padding: 30px 20px;
            border-radius: 15px;
            background: linear-gradient(135deg, #f8f9fa, #ffffff);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s ease;
        }

        .stat-item:hover {
            transform: translateY(-10px);
        }

        .stat-icon {
            font-size: 48px;
            color: #ffcc00;
            margin-bottom: 20px;
        }

        .stat-number {
            font-size: 42px;
            font-weight: 800;
            color: #002147;
            margin-bottom: 10px;
        }

        .stat-label {
            font-size: 16px;
            color: #666;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Main Content */
        .main-content {
            max-width: 1200px;
            margin: 80px auto;
            padding: 0 20px;
        }

        .content-section {
            background: #ffffff;
            border-radius: 25px;
            box-shadow: 0 25px 80px rgba(0, 0, 0, 0.1);
            margin-bottom: 60px;
            overflow: hidden;
            position: relative;
        }

        .content-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, #ffcc00, #b8860b, #002147);
        }

        .section-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0;
            align-items: center;
        }

        .section-content {
            padding: 60px;
        }

        .section-content h2 {
            font-size: 36px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 25px;
            position: relative;
        }

        .section-content h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 60px;
            height: 4px;
            background: linear-gradient(90deg, #ffcc00, #b8860b);
        }

        .section-content p {
            font-size: 18px;
            color: #555;
            line-height: 1.8;
            margin-bottom: 20px;
        }

        .section-content ul {
            list-style: none;
            padding: 0;
        }

        .section-content li {
            font-size: 16px;
            color: #666;
            margin-bottom: 12px;
            padding-left: 25px;
            position: relative;
        }

        .section-content li::before {
            content: '✓';
            position: absolute;
            left: 0;
            color: #ffcc00;
            font-weight: bold;
        }

        .section-image {
            position: relative;
            overflow: hidden;
            height: 400px;
        }

        .section-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .section-image:hover img {
            transform: scale(1.05);
        }

        /* Technology Section */
        .technology-section {
            background: linear-gradient(135deg, #002147, #003366);
            padding: 80px 0;
            color: #ffffff;
        }

        .technology-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .section-title {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-title h2 {
            font-size: 42px;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 20px;
        }

        .section-title p {
            font-size: 18px;
            color: #e0e0e0;
            max-width: 600px;
            margin: 0 auto;
        }

        .tech-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
        }

        .tech-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 40px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: transform 0.3s ease;
            text-align: center;
        }

        .tech-card:hover {
            transform: translateY(-10px);
        }

        .tech-icon {
            font-size: 48px;
            color: #ffcc00;
            margin-bottom: 25px;
        }

        .tech-card h3 {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 15px;
            color: #ffffff;
        }

        .tech-card p {
            font-size: 16px;
            line-height: 1.6;
            color: #e0e0e0;
        }

        /* Global Impact Section */
        .global-impact-section {
            background: linear-gradient(135deg, #f8f9fa, #ffffff);
            padding: 80px 0;
        }

        .impact-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .impact-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .impact-card {
            background: #ffffff;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .impact-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #ffcc00, #b8860b);
        }

        .impact-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 60px rgba(0, 0, 0, 0.15);
        }

        .impact-icon {
            font-size: 48px;
            color: #ffcc00;
            margin-bottom: 25px;
        }

        .impact-card h3 {
            font-size: 24px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 15px;
        }

        .impact-card p {
            font-size: 16px;
            line-height: 1.6;
            color: #666;
        }

        /* CTA Section */
        .cta-section {
            background: linear-gradient(135deg, #ffcc00, #b8860b);
            padding: 80px 0;
            text-align: center;
        }

        .cta-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .cta-section h2 {
            font-size: 42px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 20px;
        }

        .cta-section p {
            font-size: 18px;
            color: #333;
            margin-bottom: 40px;
            line-height: 1.6;
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .cta-btn {
            background: #002147;
            color: #ffffff;
            padding: 15px 35px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(0, 33, 71, 0.3);
        }

        .cta-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(0, 33, 71, 0.5);
            background: #001a3a;
        }

        .cta-btn.secondary {
            background: transparent;
            border: 2px solid #002147;
            color: #002147;
        }

        .cta-btn.secondary:hover {
            background: #002147;
            color: #ffffff;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 32px;
            }

            .hero p {
                font-size: 16px;
            }

            .section-grid {
                grid-template-columns: 1fr;
            }

            .section-content {
                padding: 40px 30px;
            }

            .section-content h2 {
                font-size: 28px;
            }

            .stats-container {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
                gap: 20px;
            }

            .tech-grid,
            .impact-grid {
                grid-template-columns: 1fr;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
        }

        /* Animation */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in {
            animation: fadeInUp 0.6s ease-out;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <!-- Breadcrumb -->
    <nav class="breadcrumb">
        <div class="breadcrumb-container">
            <a href="/">Home</a> > <a href="/about-us">About Us</a> > <span>Why Choose IAAI</span>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Why Choose IAAI?</h1>
            <p>Discover what makes IAAI the world's leading institution for aesthetic medicine education and professional development</p>
        </div>
    </section>

    <!-- Statistics Section -->
    <section class="stats-section">
        <div class="stats-container">
            <div class="stat-item fade-in">
                <div class="stat-icon"><i class="fas fa-users"></i></div>
                <div class="stat-number">500+</div>
                <div class="stat-label">Trained Professionals</div>
            </div>
            <div class="stat-item fade-in">
                <div class="stat-icon"><i class="fas fa-award"></i></div>
                <div class="stat-number">40+</div>
                <div class="stat-label">Training Programs</div>
            </div>
            <div class="stat-item fade-in">
                <div class="stat-icon"><i class="fas fa-globe"></i></div>
                <div class="stat-number">75+</div>
                <div class="stat-label">Countries Served</div>
            </div>
            <div class="stat-item fade-in">
                <div class="stat-icon"><i class="fas fa-star"></i></div>
                <div class="stat-number">4.9/5</div>
                <div class="stat-label">Average Rating</div>
            </div>
            <div class="stat-item fade-in">
                <div class="stat-icon"><i class="fas fa-chalkboard-teacher"></i></div>
                <div class="stat-number">15+</div>
                <div class="stat-label">Expert Instructors</div>
            </div>
            <div class="stat-item fade-in">
                <div class="stat-icon"><i class="fas fa-certificate"></i></div>
                <div class="stat-number">1000+</div>
                <div class="stat-label">Certificates Issued</div>
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Why Choose Us Section -->
        <div class="content-section fade-in">
            <div class="section-grid">
                <div class="section-content">
                    <h2>Our Distinctive Advantages</h2>
                    <p>Our commitment to excellence and comprehensive approach sets us apart in aesthetic medicine education:</p>
                    <ul>
                        <li>Internationally recognized expert instructors with decades of experience</li>
                        <li>Multiple training modalities: In-person, Live Online, and Self-paced learning</li>
                        <li>Premium global locations: Istanbul, Dubai, and Verona, Italy</li>
                        <li>Comprehensive certification programs with international accreditation</li>
                        <li>State-of-the-art facilities and latest medical technologies</li>
                        <li>Small class sizes ensuring personalized attention and mentorship</li>
                        <li>Ongoing support through our digital learning platform</li>
                        <li>Global professional network and career advancement opportunities</li>
                        <li>Comprehensive assessment and quality assurance systems</li>
                    </ul>
                </div>
                <div class="section-image">
                    <img src="/images/choose-us.jpg" alt="Why Choose IAAI">
                </div>
            </div>
        </div>
    </main>

    <!-- Technology & Innovation Section -->
    <section class="technology-section">
        <div class="technology-container">
            <div class="section-title">
                <h2>Advanced Learning Technologies</h2>
                <p>Cutting-edge educational technologies and platforms that enhance learning outcomes and professional development</p>
            </div>
            <div class="tech-grid">
                <div class="tech-card fade-in">
                    <div class="tech-icon"><i class="fas fa-laptop-code"></i></div>
                    <h3>Digital Learning Platform</h3>
                    <p>Advanced LMS with progress tracking, interactive assessments, digital certificates, and comprehensive student dashboards.</p>
                </div>
                <div class="tech-card fade-in">
                    <div class="tech-icon"><i class="fas fa-vr-cardboard"></i></div>
                    <h3>Virtual Reality Training</h3>
                    <p>Immersive VR simulations for safe practice of injection techniques and patient interaction scenarios.</p>
                </div>
                <div class="tech-card fade-in">
                    <div class="tech-icon"><i class="fas fa-mobile-alt"></i></div>
                    <h3>Mobile Learning</h3>
                    <p>Fully responsive mobile applications enabling learning anywhere, anytime with offline content access.</p>
                </div>
                <div class="tech-card fade-in">
                    <div class="tech-icon"><i class="fas fa-chart-line"></i></div>
                    <h3>Analytics & Reporting</h3>
                    <p>Comprehensive learning analytics, progress monitoring, and performance assessment tools for continuous improvement.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Global Impact Section -->
    <section class="global-impact-section">
        <div class="impact-container">
            <div class="section-title">
                <h2 style="color: #002147;">Global Impact & Recognition</h2>
                <p style="color: #666;">Transforming aesthetic medicine education worldwide through exceptional training and professional excellence</p>
            </div>
            <div class="impact-grid">
                <div class="impact-card fade-in">
                    <div class="impact-icon"><i class="fas fa-medal"></i></div>
                    <h3>International Accreditation</h3>
                    <p>Recognition by leading aesthetic medicine associations worldwide, including Europass and CPD certification.</p>
                </div>
                <div class="impact-card fade-in">
                    <div class="impact-icon"><i class="fas fa-handshake"></i></div>
                    <h3>Premier Partnerships</h3>
                    <p>Strategic collaborations with top medical institutions and healthcare organizations globally.</p>
                </div>
                <div class="impact-card fade-in">
                    <div class="impact-icon"><i class="fas fa-network-wired"></i></div>
                    <h3>Alumni Network</h3>
                    <p>Extensive professional network spanning 75+ countries across 6 continents for career advancement.</p>
                </div>
                <div class="impact-card fade-in">
                    <div class="impact-icon"><i class="fas fa-journal-whills"></i></div>
                    <h3>Research Excellence</h3>
                    <p>Featured in international medical journals and conferences, contributing to evidence-based practices.</p>
                </div>
                <div class="impact-card fade-in">
                    <div class="impact-icon"><i class="fas fa-sync-alt"></i></div>
                    <h3>Continuous Innovation</h3>
                    <p>Regular curriculum updates based on latest research, techniques, and industry developments.</p>
                </div>
                <div class="impact-card fade-in">
                    <div class="impact-icon"><i class="fas fa-shield-check"></i></div>
                    <h3>Quality Assurance</h3>
                    <p>Rigorous assessment systems and feedback mechanisms ensuring the highest educational standards.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Call to Action Section -->
    <section class="cta-section">
        <div class="cta-container">
            <h2>Experience the IAAI Advantage</h2>
            <p>Join thousands of healthcare professionals who have transformed their careers through IAAI's world-class training programs and exceptional educational experience.</p>
            <div class="cta-buttons">
                <a href="/training-programs" class="cta-btn">Explore Programs</a>
                <a href="/training-centres" class="cta-btn secondary">Our Training Centres</a>
                <a href="/contact-us" class="cta-btn secondary">Contact Us</a>
            </div>
        </div>
    </section>

    <%- include('partials/footer') %>

    <script>
        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('fade-in');
                }
            });
        }, observerOptions);

        // Observe all sections
        document.querySelectorAll('.fade-in').forEach(section => {
            observer.observe(section);
        });

        // Counter animation for stats
        function animateCounter(element, target, duration = 2000) {
            const start = 0;
            const increment = target / (duration / 16);
            let current = start;
            
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    current = target;
                    clearInterval(timer);
                }
                
                if (target > 100) {
                    element.textContent = Math.floor(current).toLocaleString() + '+';
                } else if (target < 10) {
                    element.textContent = current.toFixed(1) + '/5';
                } else {
                    element.textContent = Math.floor(current) + '+';
                }
            }, 16);
        }

        // Animate counters when they come into view
        const counterObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const counter = entry.target.querySelector('.stat-number');
                    const text = counter.textContent;
                    let target;
                    
                    if (text.includes('/5')) {
                        target = 4.9;
                    } else {
                        target = parseInt(text.replace(/\D/g, ''));
                    }
                    
                    animateCounter(counter, target);
                    counterObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.5 });

        document.querySelectorAll('.stat-item').forEach(item => {
            counterObserver.observe(item);
        });

        // Enhanced hover effects for cards
        document.querySelectorAll('.tech-card, .impact-card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-10px) scale(1.02)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });
    </script>
</body>
</html>