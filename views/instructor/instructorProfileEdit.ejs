<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= instructor.fullName %>'s Profile - IAAI</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/instructorProfile.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Montserrat:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="instructor-profile-body">
    <%- include('../partials/header') %> <nav class="breadcrumb">
        <a href="/">Home</a> > <a href="/dashboard">Dashboard</a> > <a href="/instructor/me">Instructor Profile</a> > Edit Profile
    </nav>

    <div class="profile-container">
        <% if (success_message && success_message.length > 0) { %>
            <div class="alert success"><%= success_message %></div>
        <% } %>
        <% if (error_message && error_message.length > 0) { %>
            <div class="alert error"><%= error_message %></div>
        <% } %>

        <div class="edit-profile-section">
            <h2><i class="fas fa-user-edit"></i> Edit Your Instructor Information</h2>
            <form action="/instructor/me/edit" method="POST" class="edit-form">
                <div class="form-group">
                    <label for="firstName">First Name:</label>
                    <input type="text" id="firstName" name="firstName" value="<%= instructor.firstName %>" required>
                </div>
                <div class="form-group">
                    <label for="lastName">Last Name:</label>
                    <input type="text" id="lastName" name="lastName" value="<%= instructor.lastName %>" required>
                </div>
                <div class="form-group">
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" value="<%= instructor.email %>" required readonly>
                </div>
                <div class="form-group">
                    <label for="phoneNumber">Phone Number:</label>
                    <input type="text" id="phoneNumber" name="phoneNumber" value="<%= instructor.phoneNumber || '' %>">
                </div>
                <div class="form-group">
                    <label for="bio">Biography:</label>
                    <textarea id="bio" name="bio" rows="5"><%= instructor.bio || '' %></textarea>
                </div>
                <div class="form-group">
                    <label for="designation">Designation:</label>
                    <input type="text" id="designation" name="designation" value="<%= instructor.designation || '' %>">
                </div>
                <div class="form-group">
                    <label for="experience">Experience (e.g., "10+ Years"):</label>
                    <input type="text" id="experience" name="experience" value="<%= instructor.experience || '' %>" required>
                </div>
                <div class="form-group">
                    <label for="expertise">Expertise (comma-separated):</label>
                    <input type="text" id="expertise" name="expertise" value="<%= instructor.expertise.join(', ') || '' %>">
                </div>
                <div class="form-group">
                    <label for="specializations">Specializations (comma-separated):</label>
                    <input type="text" id="specializations" name="specializations" value="<%= instructor.specializations.join(', ') || '' %>">
                </div>
                <div class="form-group">
                    <label for="teachingStyle">Teaching Style:</label>
                    <textarea id="teachingStyle" name="teachingStyle" rows="3"><%= instructor.teachingStyle || '' %></textarea>
                </div>
                <div class="form-group">
                    <label for="linkedin">LinkedIn Profile URL:</label>
                    <input type="url" id="linkedin" name="socialMedia[linkedin]" value="<%= instructor.socialMedia?.linkedin || '' %>">
                </div>
                <div class="form-group">
                    <label for="twitter">Twitter Profile URL:</label>
                    <input type="url" id="twitter" name="socialMedia[twitter]" value="<%= instructor.socialMedia?.twitter || '' %>">
                </div>
                <div class="form-group">
                    <label for="website">Personal Website URL:</label>
                    <input type="url" id="website" name="socialMedia[website]" value="<%= instructor.socialMedia?.website || '' %>">
                </div>

                <button type="submit" class="btn btn-submit">Update Profile</button>
            </form>
        </div>
    </div>

    <%- include('../partials/footer') %> </body>
</html>