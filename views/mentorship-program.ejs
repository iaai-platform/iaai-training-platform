<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mentorship Program - International Aesthetic Academic Institution</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Header Placeholder */
        .header-placeholder {
            height: 80px;
            background: #002147;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ffcc00;
            font-size: 18px;
            font-weight: 600;
        }

        /* Main Container */
        .main-container {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px 20px;
        }

        /* Construction Card */
        .construction-card {
            background: #ffffff;
            border-radius: 25px;
            padding: 60px 40px;
            text-align: center;
            box-shadow: 0 25px 80px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 100%;
            position: relative;
            overflow: hidden;
        }

        .construction-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, #ffcc00, #b8860b, #002147);
        }

        /* Construction Icon */
        .construction-icon {
            font-size: 80px;
            color: #ffcc00;
            margin-bottom: 30px;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-20px);
            }
            60% {
                transform: translateY(-10px);
            }
        }

        /* Typography */
        .construction-card h1 {
            font-size: 36px;
            font-weight: 700;
            color: #002147;
            margin-bottom: 20px;
            line-height: 1.2;
        }

        .construction-card h2 {
            font-size: 24px;
            font-weight: 600;
            color: #ffcc00;
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .construction-card p {
            font-size: 18px;
            color: #666;
            line-height: 1.8;
            margin-bottom: 30px;
        }

        /* Coming Soon Badge */
        .coming-soon-badge {
            display: inline-block;
            background: linear-gradient(135deg, #ffcc00, #b8860b);
            color: #002147;
            padding: 12px 25px;
            border-radius: 25px;
            font-size: 14px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 30px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
                box-shadow: 0 0 0 0 rgba(255, 204, 0, 0.7);
            }
            70% {
                transform: scale(1.05);
                box-shadow: 0 0 0 10px rgba(255, 204, 0, 0);
            }
            100% {
                transform: scale(1);
                box-shadow: 0 0 0 0 rgba(255, 204, 0, 0);
            }
        }

        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 30px;
        }

        .btn {
            padding: 15px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #002147, #003366);
            color: #ffffff;
            box-shadow: 0 8px 25px rgba(0, 33, 71, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(0, 33, 71, 0.5);
            background: linear-gradient(135deg, #001a3a, #002147);
        }

        .btn-secondary {
            background: transparent;
            border: 2px solid #002147;
            color: #002147;
        }

        .btn-secondary:hover {
            background: #002147;
            color: #ffffff;
            transform: translateY(-3px);
        }

        /* Features Preview */
        .features-preview {
            margin-top: 40px;
            padding-top: 30px;
            border-top: 2px dashed #e0e0e0;
        }

        .features-preview h3 {
            font-size: 20px;
            font-weight: 600;
            color: #002147;
            margin-bottom: 20px;
        }

        .features-list {
            list-style: none;
            text-align: left;
            max-width: 400px;
            margin: 0 auto;
        }

        .features-list li {
            padding: 8px 0;
            color: #666;
            font-size: 16px;
            position: relative;
            padding-left: 25px;
        }

        .features-list li::before {
            content: '✨';
            position: absolute;
            left: 0;
            color: #ffcc00;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .construction-card {
                padding: 40px 30px;
                margin: 20px;
            }

            .construction-card h1 {
                font-size: 28px;
            }

            .construction-card h2 {
                font-size: 20px;
            }

            .construction-card p {
                font-size: 16px;
            }

            .action-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 250px;
                justify-content: center;
            }
        }

        /* Loading Animation */
        .loading-dots {
            display: inline-block;
            margin-left: 10px;
        }

        .loading-dots::after {
            content: '';
            animation: loading 1.5s infinite;
        }

        @keyframes loading {
            0%, 33% { content: '.'; }
            34%, 66% { content: '..'; }
            67%, 100% { content: '...'; }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <main class="main-container">
        <div class="construction-card">
            <div class="construction-icon">
                <i class="fas fa-user-graduate"></i>
            </div>
            
            <div class="coming-soon-badge">
                Coming Soon
            </div>
            
            <h1>Mentorship Program</h1>
            <h2>Under Construction</h2>
            
            <p>We're crafting an exceptional mentorship program that will connect you with industry leaders and accelerate your aesthetic medicine career. Our comprehensive mentorship experience is currently being developed to provide you with unparalleled guidance and support.</p>
            
            <div class="features-preview">
                <h3>What to Expect:</h3>
                <ul class="features-list">
                    <li>One-on-one guidance from expert practitioners</li>
                    <li>Personalized career development planning</li>
                    <li>Advanced technique refinement sessions</li>
                    <li>Practice growth strategies and consultation</li>
                    <li>Professional network expansion opportunities</li>
                    <li>Ongoing support throughout your journey</li>
                </ul>
            </div>
            
            <div class="action-buttons">
                <a href="/training-programs" class="btn btn-primary">
                    <i class="fas fa-graduation-cap"></i>
                    View Available Programs
                </a>
                <a href="/contact-us" class="btn btn-secondary">
                    <i class="fas fa-envelope"></i>
                    Get Notified When Ready
                </a>
            </div>
            
            <p style="margin-top: 30px; font-size: 14px; color: #999;">
                <i class="fas fa-clock"></i>
                Expected Launch<span class="loading-dots"></span> <strong>Coming Soon</strong>
            </p>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script>
        // Add some interactive effects
        document.addEventListener('DOMContentLoaded', function() {
            const card = document.querySelector('.construction-card');
            const icon = document.querySelector('.construction-icon');
            
            // Add hover effect to the card
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-10px) scale(1.02)';
                icon.style.transform = 'scale(1.1)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
                icon.style.transform = 'scale(1)';
            });

            // Add click effect to buttons
            document.querySelectorAll('.btn').forEach(btn => {
                btn.addEventListener('click', function(e) {
                    this.style.transform = 'scale(0.95)';
                    setTimeout(() => {
                        this.style.transform = '';
                    }, 150);
                });
            });
        });
    </script>
</body>
</html>