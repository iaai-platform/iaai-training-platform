<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Learning Library - IAAI Training Institute</title>
  <link rel="stylesheet" href="/css/librarystyle.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  
  <style>
    /* Additional styles for master library page */
    .library-showcase {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 30px;
      margin: 60px 0;
    }
    
    .library-card {
      background: var(--bg-primary);
      border-radius: var(--border-radius-lg);
      padding: 40px 30px;
      text-align: center;
      box-shadow: var(--shadow-md);
      border: 2px solid var(--border-color);
      transition: var(--transition-smooth);
      position: relative;
      overflow: hidden;
      cursor: pointer;
    }
    
    .library-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 6px;
      background: linear-gradient(90deg, var(--accent-color), var(--secondary-color));
      transform: scaleX(0);
      transition: var(--transition-smooth);
    }
    
    .library-card:hover {
      transform: translateY(-8px);
      box-shadow: var(--shadow-lg);
      border-color: var(--accent-color);
    }
    
    .library-card:hover::before {
      transform: scaleX(1);
    }
    
    .library-icon {
      font-size: 4rem;
      background: linear-gradient(135deg, var(--accent-color), var(--secondary-color));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 25px;
      display: block;
    }
    
    .library-title {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--primary-color);
      margin-bottom: 15px;
    }
    
    .library-description {
      color: var(--text-light);
      line-height: 1.6;
      margin-bottom: 25px;
      font-size: 1rem;
    }
    
    .library-stats {
      display: flex;
      justify-content: space-around;
      margin: 25px 0;
      padding: 20px 0;
      border-top: 1px solid var(--border-color);
      border-bottom: 1px solid var(--border-color);
    }
    
    .library-stat {
      text-align: center;
    }
    
    .library-stat-number {
      font-size: 1.5rem;
      font-weight: 800;
      color: var(--accent-color);
      display: block;
    }
    
    .library-stat-label {
      font-size: 0.8rem;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    .platform-features {
      background: linear-gradient(135deg, rgba(0,33,71,0.05), rgba(111,66,193,0.05));
      padding: 60px 30px;
      border-radius: var(--border-radius-lg);
      margin: 60px 0;
      border: 2px solid rgba(111,66,193,0.1);
    }
    
    .features-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 30px;
      margin-top: 40px;
    }
    
    .feature-item {
      background: var(--bg-primary);
      padding: 30px 25px;
      border-radius: var(--border-radius-md);
      text-align: center;
      border: 2px solid var(--border-color);
      transition: var(--transition-smooth);
    }
    
    .feature-item:hover {
      transform: translateY(-4px);
      border-color: var(--accent-color);
      box-shadow: var(--shadow-md);
    }
    
    .feature-icon {
      font-size: 3rem;
      color: var(--accent-color);
      margin-bottom: 20px;
    }
    
    .feature-title {
      font-size: 1.3rem;
      font-weight: 600;
      color: var(--primary-color);
      margin-bottom: 15px;
    }
    
    .feature-description {
      color: var(--text-light);
      line-height: 1.6;
      font-size: 0.95rem;
    }
    
    .getting-started {
      background: var(--bg-primary);
      padding: 50px;
      border-radius: var(--border-radius-lg);
      margin: 60px 0;
      box-shadow: var(--shadow-md);
      border: 2px solid var(--border-color);
    }
    
    .steps-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
      margin-top: 40px;
    }
    
    .step-item {
      text-align: center;
      position: relative;
    }
    
    .step-number {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--accent-color), var(--secondary-color));
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      font-weight: 800;
      margin: 0 auto 20px;
      box-shadow: 0 4px 15px rgba(111, 66, 193, 0.3);
    }
    
    .step-title {
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--primary-color);
      margin-bottom: 10px;
    }
    
    .step-description {
      color: var(--text-light);
      line-height: 1.6;
      font-size: 0.95rem;
    }
    
    .quick-access {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
      margin: 40px 0;
    }
    
    .quick-access-btn {
      padding: 20px;
      background: var(--bg-primary);
      border: 2px solid var(--border-color);
      border-radius: var(--border-radius-md);
      text-decoration: none;
      color: var(--text-dark);
      text-align: center;
      transition: var(--transition-smooth);
    }
    
    .quick-access-btn:hover {
      border-color: var(--accent-color);
      transform: translateY(-3px);
      box-shadow: var(--shadow-md);
      color: var(--accent-color);
    }
    
    .quick-access-icon {
      font-size: 2rem;
      margin-bottom: 10px;
      display: block;
    }
    
    .progress-overview {
      background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
      color: white;
      padding: 60px 40px;
      border-radius: var(--border-radius-lg);
      margin: 60px 0;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .progress-overview::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('/images/hero-background2.jpg') center/cover;
      opacity: 0.1;
      z-index: 0;
    }
    
    .progress-content {
      position: relative;
      z-index: 2;
    }
    
    .progress-title {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 20px;
      text-shadow: 0 4px 8px rgba(0,0,0,0.3);
    }
    
    .progress-subtitle {
      font-size: 1.3rem;
      opacity: 0.9;
      margin-bottom: 40px;
    }
    
    .progress-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 30px;
      margin-top: 40px;
    }
    
    .progress-item {
      background: rgba(255,255,255,0.1);
      padding: 30px 20px;
      border-radius: var(--border-radius-md);
      border: 2px solid rgba(255,255,255,0.2);
      backdrop-filter: blur(10px);
    }
    
    .progress-number {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 10px;
      display: block;
    }
    
    .progress-label {
      font-size: 1rem;
      font-weight: 500;
      opacity: 0.9;
    }
    
    @media (max-width: 768px) {
      .library-showcase {
        grid-template-columns: 1fr;
        gap: 20px;
        margin: 40px 0;
      }
      
      .library-card {
        padding: 30px 20px;
      }
      
      .library-stats {
        flex-direction: column;
        gap: 15px;
      }
      
      .platform-features,
      .getting-started,
      .progress-overview {
        padding: 40px 20px;
        margin: 40px 0;
      }
      
      .features-grid,
      .steps-container {
        grid-template-columns: 1fr;
        gap: 20px;
      }
      
      .progress-title {
        font-size: 2.5rem;
      }
      
      .progress-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 20px;
      }
    }
    
    @media (max-width: 480px) {
      .progress-grid {
        grid-template-columns: 1fr;
      }
      
      .quick-access {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>
  <%- include('partials/header') %>

  <!-- Hero Section -->
  <section id="hero">
    <div class="hero-content">
      <h1><i class="fas fa-graduation-cap"></i> Your Learning Library</h1>
      <p>Welcome to your comprehensive training hub - Access all your courses, track progress, and advance your aesthetic medicine career with our integrated learning platform.</p>
    </div>
  </section>



    <!-- Platform Features -->
    <div class="platform-features">
      <div class="section-header">
        <h2>Platform Capabilities & Strengths</h2>
        <p>Discover what makes IAAI's learning platform the premier choice for aesthetic medicine professionals worldwide</p>
      </div>
      
      <div class="features-grid">
        <div class="feature-item">
          <i class="fas fa-brain feature-icon"></i>
          <h4 class="feature-title">AI-Powered Learning</h4>
          <p class="feature-description">
            Adaptive learning paths that adjust to your pace and learning style, ensuring optimal knowledge retention and skill development.
          </p>
        </div>
        
        <div class="feature-item">
          <i class="fas fa-certificate feature-icon"></i>
          <h4 class="feature-title">Instant Certification</h4>
          <p class="feature-description">
            Automated certificate generation upon course completion with blockchain verification for industry-recognized credentials.
          </p>
        </div>
        
        <div class="feature-item">
          <i class="fas fa-mobile-alt feature-icon"></i>
          <h4 class="feature-title">Multi-Device Access</h4>
          <p class="feature-description">
            Seamless learning experience across desktop, tablet, and mobile devices with offline content synchronization.
          </p>
        </div>
        
        <div class="feature-item">
          <i class="fas fa-clock feature-icon"></i>
          <h4 class="feature-title">Progress Tracking</h4>
          <p class="feature-description">
            Real-time progress monitoring with detailed analytics, completion rates, and personalized learning recommendations.
          </p>
        </div>
        
        <div class="feature-item">
          <i class="fas fa-globe feature-icon"></i>
          <h4 class="feature-title">Global Timezone Support</h4>
          <p class="feature-description">
            Intelligent timezone conversion for live sessions and automatic scheduling adjustments for international learners.
          </p>
        </div>
        
        <div class="feature-item">
          <i class="fas fa-link feature-icon"></i>
          <h4 class="feature-title">Integrated Training Paths</h4>
          <p class="feature-description">
            Seamlessly linked online and in-person components for comprehensive training programs with unified certification.
          </p>
        </div>
      </div>
    </div>

    <!-- Getting Started Guide -->
    <div class="getting-started">
      <div class="section-header">
        <h2>How to Use Your Library</h2>
        <p>Master your learning journey with our step-by-step guide to maximizing your training experience</p>
      </div>
      
      <div class="steps-container">
        <div class="step-item">
          <div class="step-number">1</div>
          <h4 class="step-title">Choose Your Path</h4>
          <p class="step-description">
            Select from self-paced, live online, or in-person training based on your schedule, learning style, and career goals.
          </p>
        </div>
        
        <div class="step-item">
          <div class="step-number">2</div>
          <h4 class="step-title">Track Progress</h4>
          <p class="step-description">
            Monitor your advancement through comprehensive dashboards showing completion rates, scores, and time invested.
          </p>
        </div>
        
        <div class="step-item">
          <div class="step-number">3</div>
          <h4 class="step-title">Complete Assessments</h4>
          <p class="step-description">
            Demonstrate mastery through interactive quizzes, practical examinations, and attendance confirmations.
          </p>
        </div>
        
        <div class="step-item">
          <div class="step-number">4</div>
          <h4 class="step-title">Earn Certificates</h4>
          <p class="step-description">
            Receive industry-recognized certifications instantly upon completion, with shareable verification links.
          </p>
        </div>
      </div>
    </div>

    <!-- Quick Access Tools -->
   <!-- <div class="section-header">
      <h2>Quick Access Tools</h2>
      <p>Streamline your learning experience with these convenient shortcuts</p>
    </div>
    
    <div class="quick-access">
      <a href="/certificates" class="quick-access-btn">
        <i class="fas fa-award quick-access-icon"></i>
        <strong>My Certificates</strong>
        <small>View & Download</small>
      </a>
      
      <a href="/schedule" class="quick-access-btn">
        <i class="fas fa-calendar-alt quick-access-icon"></i>
        <strong>Upcoming Sessions</strong>
        <small>Live Classes</small>
      </a>
      
      <a href="/progress" class="quick-access-btn">
        <i class="fas fa-chart-line quick-access-icon"></i>
        <strong>Progress Report</strong>
        <small>Detailed Analytics</small>
      </a>
      
      <a href="/support" class="quick-access-btn">
        <i class="fas fa-headset quick-access-icon"></i>
        <strong>Learning Support</strong>
        <small>Get Help</small>
      </a>
    </div> -->

   
  <!-- Library Introduction -->
  <section id="library-content">
    <div class="section-header">
      <h2>Your Complete Training Ecosystem</h2>
      <p>Experience the future of aesthetic medicine education with our comprehensive, multi-modal learning platform designed for busy professionals</p>
    </div>

    <!-- Library Types Showcase -->
    <div class="library-showcase">
      <!-- Self-Paced Online Training -->
      <div class="library-card" onclick="window.location.href='/library/self-paced'">
        <i class="fas fa-play-circle library-icon"></i>
        <h3 class="library-title">Self-Paced Learning</h3>
        <p class="library-description">
          Learn at your own pace with our comprehensive video-based courses. Perfect for busy professionals who need flexibility in their learning schedule.
        </p>
        <div class="library-stats">
          <div class="library-stat">
            <span class="library-stat-number">12</span>
            <span class="library-stat-label">Courses</span>
          </div>
          <div class="library-stat">
            <span class="library-stat-number">89</span>
            <span class="library-stat-label">Videos</span>
          </div>
          <div class="library-stat">
            <span class="library-stat-number">32h</span>
            <span class="library-stat-label">Content</span>
          </div>
        </div>
        <div class="cta-btn btn-primary" style="display: inline-flex; margin: 0;">
          <i class="fas fa-arrow-right"></i> Access Self-Paced Library
        </div>
      </div>

      <!-- Live Online Training -->
      <div class="library-card" onclick="window.location.href='/library/live'">
        <i class="fas fa-video library-icon"></i>
        <h3 class="library-title">Live Online Training</h3>
        <p class="library-description">
          Join interactive live sessions with expert instructors and fellow professionals. Real-time learning with immediate feedback and networking opportunities.
        </p>
        <div class="library-stats">
          <div class="library-stat">
            <span class="library-stat-number">8</span>
            <span class="library-stat-label">Sessions</span>
          </div>
          <div class="library-stat">
            <span class="library-stat-number">3</span>
            <span class="library-stat-label">Upcoming</span>
          </div>
          <div class="library-stat">
            <span class="library-stat-number">24h</span>
            <span class="library-stat-label">Live Time</span>
          </div>
        </div>
        <div class="cta-btn btn-primary" style="display: inline-flex; margin: 0;">
          <i class="fas fa-arrow-right"></i> Access Live Library
        </div>
      </div>

      <!-- In-Person Training -->
      <div class="library-card" onclick="window.location.href='/library/in-person'">
        <i class="fas fa-users library-icon"></i>
        <h3 class="library-title">In-Person Training</h3>
        <p class="library-description">
          Hands-on practical training with real patients and advanced equipment. Master techniques through direct supervision and immediate feedback.
        </p>
        <div class="library-stats">
          <div class="library-stat">
            <span class="library-stat-number">4</span>
            <span class="library-stat-label">Courses</span>
          </div>
          <div class="library-stat">
            <span class="library-stat-number">2</span>
            <span class="library-stat-label">Attended</span>
          </div>
          <div class="library-stat">
            <span class="library-stat-number">16h</span>
            <span class="library-stat-label">Hands-On</span>
          </div>
        </div>
        <div class="cta-btn btn-primary" style="display: inline-flex; margin: 0;">
          <i class="fas fa-arrow-right"></i> Access In-Person Library
        </div>
      </div>
    </div>


   
  </section>

  <!-- JavaScript for Interactivity -->
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Add hover effects and animations
      const libraryCards = document.querySelectorAll('.library-card');
      const featureItems = document.querySelectorAll('.feature-item');
      
      // Animate stats on scroll
      const observerOptions = {
        threshold: 0.5,
        rootMargin: '0px 0px -100px 0px'
      };
      
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.style.animation = 'fadeInUp 0.8s ease-out forwards';
          }
        });
      }, observerOptions);
      
      // Observe feature items for scroll animations
      featureItems.forEach(item => {
        observer.observe(item);
      });
      
      // Add click tracking for library cards
      libraryCards.forEach(card => {
        card.addEventListener('click', function() {
          // Add click animation
          this.style.transform = 'scale(0.98)';
          setTimeout(() => {
            this.style.transform = '';
          }, 150);
        });
      });
      
      // Smooth scroll for internal links
      document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
          e.preventDefault();
          const target = document.querySelector(this.getAttribute('href'));
          if (target) {
            target.scrollIntoView({
              behavior: 'smooth',
              block: 'start'
            });
          }
        });
      });
      
      // Add loading states for external navigation
      document.querySelectorAll('a[href^="/library/"]').forEach(link => {
        link.addEventListener('click', function(e) {
          const btn = this.querySelector('.cta-btn');
          if (btn) {
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Loading...';
            btn.style.opacity = '0.8';
          }
        });
      });
      
      console.log('🎓 IAAI Master Library initialized successfully');
    });
  </script>

  <%- include('partials/footer') %>
</body>
</html>